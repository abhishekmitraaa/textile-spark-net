- 2026-07-25: **Search results page (buyer) — Brand-tab Follow now persists.** New `src/lib/brandFollowStore.ts` (module-level `Set<id>` + `useSyncExternalStore` + `localStorage`, `cosora.brandFollows.v1`; exports `useBrandFollows()` / `toggleBrandFollow(id)`) — same store pattern as the other buyer stores. `BrandCard` swapped its throwaway `useState(false)` for `useBrandFollows()` + `toggleBrandFollow(brand.id)`, so a Follow survives navigation and reloads. Kept **separate** from `followingStore` on purpose: the Search Brand tab's suppliers (`BRAND_RESULTS`) have their own ids and rich records don't exist in that store, and following them here shouldn't inject them into the Following page. Verified (Playwright): Follow → "Following", full reload → still "Following" (muted outline). Build clean. (Note: the brand records themselves remain curated mock data; only the follow flag is persisted.)

- 2026-07-25: **Search results page (buyer) — de-cluttered the Brand-tab cards (`BrandCard` redesign).** `src/pages/SearchResults.tsx`. The card was visually noisy (TrustedSEAL image badge, a dotted 4-item meta line that wrapped, 3 filled-coral deal chips, per-tile borders, and 3 action buttons). Rebuilt with a calm hierarchy: **identity** (logo · name + small coral `BadgeCheck` verified · muted `type · location`) → **one quiet stats line** (★rating (reviews) · followers, with a right-aligned "View all ›") → **unboxed product tiles** (bigger rounded image + name + `price · MOQ`, hover-zoom, no nested borders) → **two actions** (Call Now filled + Chat outline). Dropped the deal chips and the redundant third "View all" button; **Follow** became an outline-coral pill (was solid) to cut the coral overload; card gap bumped `gap-3→gap-4 lg:gap-5`. Restrained accent usage per the design skill (one filled-coral CTA + coral price, everything else neutral).
  - **Image-quality fixes (round 2):** several Unsplash IDs I'd trusted from memory actually render as **non-garments** — a sneaker (`1551489186…`, was on "Oxford Shirt" + Brand-Picks bp1 "Shirts"), a cap (`1622445275576…`, "Graphic Print Tee"), a shoe flat-lay (`1525507119028…`, "Ribbed Tank Top"), plus two unverified dress IDs. Locked **every** brand logo + product image to the ~13 IDs I visually confirmed render as apparel this session, and reordered a couple so no two identical photos sit adjacent in a rail. Every tile is now a real garment.
  - **Verified** (Playwright, live :8080): q=shirt → 3 clean supplier cards, all tiles garments (Oxford Shirt = shirts, not a sneaker); q=jacket → jacket imagery; q=dress → single Aurelia card + Submit-Requirement CTA reads intentional, no adjacent-duplicate photos; mobile single-column clean. `npm run build` clean.

- 2026-07-25: **Search results page (buyer) — tightened the desktop filter bar + fully redesigned the Brand tab.** `src/pages/SearchResults.tsx`.
  - **Desktop bar tightened.** The GENDER|SORT|FILTER|grid bar no longer stretches edge-to-edge on wide screens. Mobile/tablet keep the evenly-segmented grid; at `lg` the row switches to `grid lg:flex` — the three controls cluster left (with `lg:px-5 lg:text-sm`, dividers between them) and the density toggle is pushed right via `lg:ml-auto`.
  - **Brand tab redesigned into a supplier-discovery surface.** Replaced the old 2-row name+followers list with a **relevance-filtered grid of rich `BrandCard`s** (`grid-cols-1 lg:grid-cols-2`, framer stagger). New data model: `BRAND_RESULTS: BrandResult[]` — 6 apparel manufacturers/exporters, each tagged with what they **`deals` in** and a `products[]` line, deliberately overlapping categories so one query surfaces several competing suppliers. `matchBrand(brand, tokens)` returns whether a brand is relevant (query token in name / deals / any product name) and its product line **query-matching-first**; `brandResults` filters to hits and drives the "Brand N" tab count + a "**N brands deal in "{query}"**" header.
    - **`BrandCard`**: logo, name + **TrustedSEAL** image, `type · location · ★rating (reviews) · followers`, coral **deal chips**, **Follow** toggle (local state, per the VendorProfile precedent), a horizontal **product rail** of related items (image · price · MOQ, each → `/product/:id`, `useDragScroll` for desktop mouse), and **Call Now** (`useCallVendor`) / **Chat** (`/chats`) / **View all** (`/vendor/:id`) actions.
    - **Empty state**: "No brands match …" + the `SubmitRequirementCard` (with Quick RFQ). Also appended a Submit-Requirement CTA under the results. Removed the now-unused `BadgeCheck` import; the old `brands` name-only memo is gone.
  - **Image quality fix**: initial `BRAND_RESULTS` used some Unsplash photo IDs from memory — one rendered a **dog on a beach** for "Flannel Shirt". Remapped every brand logo + product image to the **fashion Unsplash IDs already proven in this repo** (New Arrivals / video close-ups / brand picks), and placed the two known jacket photos on Everest's most-visible jacket products so nothing shows a stray non-apparel image.
  - **Verified** (Playwright, live :8080): desktop bar clusters left with toggle right; Brand tab at q=shirt → 3 supplier cards leading with shirt products, q=jacket → Everest/Denim/FitForm leading with jacket products; mobile → single-column cards + bottom Submit Requirement card; all imagery apparel. `npm run build` clean.

- 2026-07-25: **Search results page (buyer) — reverted the desktop filter *sidebar* back to a top-bar dropdown + added a GENDER selector.** `src/pages/SearchResults.tsx`. Per follow-up: the persistent desktop `aside` sidebar was removed; filtering is again a bar of dropdowns/actions shown on **every** breakpoint (mobile + desktop): **GENDER | SORT | FILTER | grid-toggle**. FILTER opens the existing context-aware bottom sheet (`FilterSheet`).
  - **Gender moved out of the sheet into its own bar dropdown** (`Menu` type gained `"gender"`; new `FilterPanel` with "Gender / Age" → Everyone / Men / Women / Boys / Girls). Options come from the context-aware `genderFacet` (e.g. dresses → Women/Girls); the GENDER button hides for non-apparel searches with no gender dimension. Writes `selections.gender` (single-select; "Everyone" clears it) — same key `matchFacet` already filters on, and the query-implied gender pre-scope still seeds it. The sheet + its FILTER active-count now use `sheetFacets` (= `schema.facets` minus gender) so gender isn't duplicated or double-counted.
  - **Removed** the desktop-only `aside` sidebar, the `hidden lg:flex` results toolbar, and the now-unused `DesktopSort` component; the products branch is a single-column `<>` again (grid full width). Kept the other desktop work intact — wider `xl:max-w-7xl` container, readable `lg:` card type, enlarged Brand Picks rail, related Video Close-Ups, and the interleaved Submit Requirement cards. `FacetControls` stays (still the sheet body).
  - **Verified** (Playwright, live :8080, q=Windbreaker Jackets): desktop + mobile both show the GENDER|SORT|FILTER|grid bar; GENDER dropdown lists Everyone/Men/Women/Boys/Girls with coral active state; FILTER opens the sheet showing Fabric/Type/Pattern/Colour/Price/MOQ (no Gender) + "Show 28 results"; no sidebar. `npm run build` clean.

- 2026-07-25: **Category images — self-hosted, background-removed cutouts across New Arrivals, the brand page, and the browse taxonomy (replaced remote Unsplash hotlinks).** Sourced clean packshot/model photos from Unsplash (free license), background-removed them locally with Python `rembg` (u2net) into trimmed, transparent, square PNGs, and self-hosted everything under `src/assets/categories/`. Per the repo image-import rule, all are wired via ES-module imports (never path strings).
  - **New Arrivals — 12 category tiles now transparent cutouts.** `src/pages/NewArrivals.tsx`: added imports for `@/assets/categories/{womens-apparel,mens-apparel,mens-jeans,mens-shirt,accessories,womens-trousers,womens-tshirts,womens-shoes,kidswear,ethnic-wear,activewear,winter-wear}.png` and pointed the `CATEGORIES` array at them (was 12 `images.unsplash.com/...` URLs). The tile already renders `object-contain` on a white padded card, so the cutouts drop in cleanly. Rule learned: model shots + single-object packshots cut out cleanly; busy flat-lays don't — six weak flat-lays were re-sourced (e.g. accessories→red handbag, womens-trousers→pink joggers, mens-shirt→denim shirt, womens-apparel→white-dress model).
  - **Brand page (`/vendor/:id`) — 6 capability tiles now real cutouts.** `src/pages/VendorProfile.tsx`: removed the 6 placeholder brand-logo imports (`brands/*.png` were being reused as category art) and replaced with `@/assets/categories/brand/{tshirt,denim,printing,manufacturing,embroidery,finishing}.png` (T-Shirt/Denim = garment packshots; Printing = graphic tee; Manufacturing = Singer sewing machine; Embroidery = thread-spool tray; Finishing = steam iron). Switched the tile from `object-cover` on `bg-gray-50` to `object-contain` on `bg-white p-2` so transparent cutouts sit correctly.
  - **Browse taxonomy — 11 top-category banners self-hosted (NOT cut out).** `src/lib/categoryTaxonomy.ts`: the Categories page renders each category `image` as a full-bleed **cover banner** with a dark gradient + text overlay, where a transparent cutout would look broken — so these stay photos. Downloaded the 11 banner photos to `@/assets/categories/banners/*.jpg`, imported them, and set each top category's `image:` to its local banner (the `images:` subcategory pools remain the existing remote Unsplash URLs). Also upgraded two poor banners in passing: `raw-materials` (was defocused bokeh → now fabric bolts) and `trims-home` (was a floral handbag → now folded home linens).
  - **Pipeline / verification.** Reusable `rembg` download→cutout→contact-sheet script; every image visually reviewed on a white tile (the real render context) before wiring. `npm run build` clean (only the pre-existing ~2.5MB bundle-size warning). Note: `src/assets/categories/` adds ~5.4MB of source PNG/JPG (Vite emits them as separate hashed assets, not inlined) — fine to optimize/downscale later if needed.

- 2026-07-25: **Brand page (`/vendor/:id`) — desktop widened from a phone-width strip; mobile unchanged.** `src/pages/VendorProfile.tsx`. Container + sticky header `lg:max-w-3xl → lg:max-w-5xl` (768→1024px) with `lg:px-4`; product/media grids gain desktop columns (Recommendations + Videos `lg:grid-cols-6`, Catalogues `lg:grid-cols-4`, All-Products 2-col `lg:grid-cols-3 xl:grid-cols-4` / 3-col `lg:grid-cols-5 xl:grid-cols-6`); Recommendations empty-state `col-span-3 → col-span-full`. All `lg:`/`xl:`-gated; mobile byte-identical. (Sibling `BusinessProfile.tsx` left on the narrow `lg:max-w-3xl` for now.)

- 2026-07-25: **Support chat — "Cosora Team" header, no call button, agent-connected indicator.** `src/pages/SupportChat.tsx`. Per request: (1) removed the call/phone button from the header (and the now-unused `Phone` import + `tel:` handler); (2) header now shows **Cosora Team** (avatar initial "C", status just "Online") instead of an individual agent name; (3) the chat now **indicates which agent connected** — added a `kind: "system"` message type rendered as a centered green pill ("Abdul from the Cosora Team joined the chat") as the first item, and reworded the greeting to "Hi, I'm Abdul from the Cosora Team. How can I help you with your enquiry today?" (support bubbles still carry the "Abdul" label). `showQuickPrompts` switched from `messages.length === 1` to `!messages.some(m => m.sender === "user")` since the initial thread now has 2 seeded messages. Verified (Playwright 1200 + 390px): header reads "Cosora Team · Online" with no call button (asserted absent), the green "joined" pill shows above Abdul's greeting, quick-reply chips still render. Build clean.

- 2026-07-25: **Support chat — user-bubble contrast bumped to WCAG AA.** `src/pages/SupportChat.tsx`. The filled coral user bubble was brand `#ef4d62`, which gives white message text only ~3.6:1 (under the 4.5:1 AA bar for small text). Deepened just the bubble gradient to `linear-gradient(180deg, #da3651 0%, #c62f49 100%)` — ~4.5:1 at its lightest point, still clearly the same coral/rose family. Header avatar, send button, quick-reply chips, and the support "Abdul" label stay brand `#ef4d62` (buttons/avatar match the rest of the buyer app; only the large text-fill bubble goes a shade deeper where legibility matters). Verified visually at 1200px; build clean.

- 2026-07-25: **Support chat (`/profile/help/chat`) — fixed back navigation + full redesign (used the `design-taste-frontend` skill; product-UI read, coral `#ef4d62` locked, motion motivated).** `src/pages/SupportChat.tsx`.
  - **Back bug fix.** The header back button was hardcoded `navigate("/profile/help")`, so coming from Profile → "Chat with Us" and pressing Back landed on the Help Center instead of Profile. Replaced with a smart `goBack()` that uses React Router's history index (`window.history.state.idx`): `idx > 0 → navigate(-1)` (returns to wherever the user came from — Profile), else `navigate("/profile")` as a deep-link/refresh fallback. Verified via Playwright: Profile → chat → Back lands on `/profile`. (The route is only reached from Profile's "Chat with Us" row; the Help page uses its own inline `ChatModal`, not this route.)
  - **Redesign — all existing functions preserved** (text send, Enter-to-send, Camera/Gallery/Audio/PDF attachments via hidden file inputs, canned support replies + typing indicator, tel: call button, timestamps, and the **legally-required** `CHAT_MONITORING_NOTICE` amber disclosure — kept verbatim). New: **desktop chat-card** (mobile is edge-to-edge `h-[100dvh]`; `lg:` centers a `max-w-2xl` rounded/shadowed card on a slate backdrop), header **avatar with online-dot ring** + "Online · …" status, a **"Today" date pill**, **support avatars** beside white bubbles, **coral-gradient user bubbles** with **read ticks** (`CheckCheck`), framer-motion **message-enter animation** (respects `useReducedMotion`), **rich attachment previews** (image thumbnail / native `<audio>` player / file chip with open-link — object URLs created in `handleFile` and revoked on unmount), a 4-up **attachment grid** (was a flat row), **first-run quick-reply chips** (Track my order / Payment or refund help / Verification help / Talk to a human → send on tap, hidden once the user sends anything), and a softer pill input (`bg-gray-100` → focus ring). `send()` refactored to accept optional text so chips reuse it.
  - **Verified** (Playwright at 1440 + 390px against `vite preview`): desktop card + conversation (sent a message, got the canned reply, opened the attach grid), mobile full-screen initial state, and the back-nav assertion. Build clean; removed an unused `Play` import; `scrollbar-hide` confirmed defined in `src/index.css`.

- 2026-07-25: **Buyer Profile — desktop scaled up ("make it look a little bigger"); mobile unchanged.** `src/pages/Profile.tsx`. Follow-up to the layout entry below. All bumps `lg:`-gated. Container `lg:max-w-5xl → lg:max-w-6xl`, left rail `340px → 380px`, grid gap `lg:gap-6 → lg:gap-8`. Header "My Profile" `lg:text-2xl → lg:text-3xl`. Identity card: `lg:p-6`, avatar `lg:w-24 lg:h-24`, camera badge `lg:w-8 lg:h-8`, name `lg:text-xl` (chose `xl` not `2xl` — `2xl` truncated the name to "…" in the 380px rail), business `lg:text-base`, meta row + email/phone `lg:text-[15px]`, Edit pill `lg:px-4 lg:py-2 lg:text-sm`. Stats (`StatCell`): number `lg:text-xl`, icon `lg:w-6 lg:h-6`. Interests card `lg:p-5`, heading `lg:text-base`, chips `lg:text-sm lg:px-3 lg:py-1.5`. List rows (`Row`): `lg:px-5 lg:py-4`, icon tile `lg:w-10 lg:h-10`, label `lg:text-[15px]`, chevron `lg:w-5 lg:h-5`. `SectionLabel` `lg:text-xs lg:mt-7`. **Verified** (Playwright 1440 + 390px; temporarily bypassed the signed-out guard to render the authenticated view with placeholder data, then reverted — confirmed no `&& false`/marker remains): desktop reads noticeably larger and balanced, mobile byte-identical. Build clean.

- 2026-07-25: **Buyer Profile, Social Links & Help Center — desktop layouts optimized; mobile unchanged (used the `design-taste-frontend` skill; product-UI read, VARIANCE 4 / MOTION 3 / DENSITY 4, accent locked to buyer coral `#ef4d62`).** All three were `max-w-2xl` single columns that read as a stretched phone on desktop (per the user's screenshot).
  - **`src/pages/Profile.tsx` — two-column desktop, mobile byte-identical.** Container widened `max-w-2xl → lg:max-w-5xl` (+`lg:px-6 lg:pt-6`, title `lg:text-2xl`). Wrapped the body in a `contents lg:grid lg:grid-cols-[minmax(0,340px)_minmax(0,1fr)] lg:gap-6 lg:items-start`, with a **sticky left rail** (`contents lg:block lg:sticky lg:top-20` holding profile card + stats + interests) and a **right main** column (`contents lg:block` holding Activity / Account Settings / Help & Support lists). The `display:contents` wrappers collapse out of the box tree below `lg`, so the mobile stack renders from the original markup unchanged — no reordering, no spacing drift. `SectionLabel` gained an optional `className` (first "Activity" label gets `lg:mt-0` to top-align the columns). Legal + logout stay full-width below the grid; logout button constrained `lg:max-w-xs lg:mx-auto` so it doesn't stretch.
  - **`src/pages/Help.tsx` — contact rail (left) + FAQ/guides (right) on desktop; mobile unchanged.** Container `max-w-2xl → lg:max-w-6xl`. Welcome heading and "Still need help?" band stay full-width; the middle sections split into a `lg:grid lg:grid-cols-[340px_minmax(0,1fr)]` with a **sticky left rail** (Contact Us / Follow Us / Email / Delete Account) and **right main** (FAQ dropdown + Quick Guides). DOM order is preserved (contact content already precedes FAQ), so no grid line-placement tricks needed. The two group wrappers are `motion.div`s carrying `variants={reduced ? {} : page}` (no transform props) so the existing Framer stagger still propagates through the new nesting **and** no transformed ancestor breaks the sticky rail. Colors/chat modal left as-is to keep mobile visual parity. All new structure `lg:`-gated.
  - **`src/pages/ProfileSocialLinks.tsx` — redesigned (both breakpoints).** Replaced the plain single card of stacked inputs with a `bg-gray-50` page: an intro card (coral `Share2` tile + live "N of 6 added" count), then a responsive `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` of per-channel cards — each with a brand-tinted icon tile (Facebook `#1877F2`, LinkedIn `#0A66C2`, YouTube `#FF0000`, Instagram `#E4405F`, X near-black, Other grey), label + hint, a green "Added" badge when filled, and an `ExternalLink` "open in new tab" affordance (`normalizeUrl` prefixes `https://` / strips leading `@`). Footer action bar shows a ready-to-save count beside the coral Update button. Save/DB logic (`saveSetting` / `updateSocial`) unchanged.
  - **Verified** (Playwright at 1440px + 390px against `vite preview`): Social desktop = 3-col card grid + intro + right-aligned action bar; Help desktop = sticky contact rail left, FAQ/guides right, full-width "Still need help?" band; both mobiles are clean single-column stacks in original order. Profile requires auth (screenshot showed the signed-out prompt) — its `lg:`-gated / `display:contents` structure was confirmed by code review + a clean `vite build`. Build clean (only the pre-existing ~2.5MB bundle-size warning); no new typecheck errors in the three edited files.

- 2026-07-25: **Search results page (buyer) — bigger Brand Picks + related Video Close-Ups + interleaved "Submit Requirement" (both breakpoints).** `src/pages/SearchResults.tsx`. Three additive features requested on top of the desktop-sidebar pass below.
  - **Brand Picks enlarged:** was small cards (mobile `w-32` rail / desktop 3-col horizontal thumbnails). Now large vertical cards in a drag-scrollable rail on both breakpoints (`w-40 lg:w-52`, portrait `aspect-[3/4]` image, name/category/price + Call Now, coral sponsored accent). Replaced the 3 picsum **scenery** placeholders with **6 real fashion Unsplash images** (so the sponsored rail reads as merchandise and the enlarged cards fill the desktop width and still scroll). Added `useDragScroll` for desktop mouse panning.
  - **Video Close-Ups related to the search:** new rail (reuses the New Arrivals reel pattern — `Play` badge, gradient, category/price caption) that opens the shared `VideoCloseUpsViewer`. Data from `useVideoCloseUps()` with `VIDEO_CLOSE_UPS` fallback; a `relatedVideos` memo ranks videos whose `category`/`brandLine`/`brandName` match the query tokens first (stable order, always returns the full set so the rail is never empty). Heading shows the context: "Video Close-Ups · {query}".
  - **"Post your requirement" interleaved like New Arrivals:** `SubmitRequirementCard` (with `onQuickRfq` quick-action rows) now injected into the product grid every 5 rows per breakpoint — `feedNodes` inserts a `col-span-full lg:max-w-4xl lg:mx-auto` card at each `cols*10` (desktop) multiple plus a `lg:hidden` card at each `cols*5` (mobile) multiple, so mobile sees a card every 5 mobile rows and desktop every 5 desktop rows with no dead cells. Wired `QuickRfqModal` (Quick RFQ) + mounted `VideoCloseUpsViewer` alongside the existing `FilterSheet`. Kept the product-card `motion`/`listItem` stagger by pushing each card as a `motion.div` into `feedNodes`.
  - **Verified** (Playwright, live :8080 catalogue): desktop (q=Jeans, 28 results) — big Brand Picks rail with real product images, "Video Close-Ups · Jeans" leading with denim clips (ranking works), Jeans-specific sidebar facets; desktop (q=Windbreaker Jackets) — full-width Submit Requirement card at row 20 with sidebar still sticky; mobile — enlarged Brand Picks, video rail, and the interleaved requirement card (Submit + Quick RFQ / Create New / My Previous Quotes) at row 10, SORT/FILTER bar + bottom nav intact. `npm run build` clean.

- 2026-07-25: **Search results page (buyer) — desktop optimised into a marketplace-standard sidebar layout; mobile unchanged.** `src/pages/SearchResults.tsx`. The page was mobile-first (`max-w-6xl`, a full-width SORT/FILTER/grid bar, a lonely horizontal Brand Picks rail, tiny 8–10px card type, and a bottom-sheet filter) so on desktop it read as a stretched phone. All new structure is `lg:`-gated; mobile (and tablet <1024) is byte-for-byte the original.
  - **Persistent filter sidebar (≥lg):** the product-tab content is now `lg:flex lg:gap-6 lg:items-start` — a `hidden lg:block lg:w-56 xl:w-64` sticky `aside` (`lg:sticky lg:top-[128px]`, `max-h-[calc(100vh-144px)] overflow-y-auto`) holding the facets + a "Clear (n)" action, beside a `lg:flex-1` results column. Extracted the facet inputs from `FilterSheet` into a shared `FacetControls` component used by **both** the mobile bottom sheet and the desktop sidebar (single source of truth for colour swatches / price slider / chips).
  - **Desktop results toolbar:** `hidden lg:flex` row with a live result count, a new anchored `DesktopSort` popover (replaces the full-width mobile sort panel on desktop), and the 2/3-col density toggle. The old mobile SORT/FILTER/grid bar got `lg:hidden` (kept intact for mobile/tablet + its bottom sheet).
  - **Brand Picks:** horizontal scroll rail on mobile → `lg:grid lg:grid-cols-3` full-width row of horizontal sponsored cards (image left / info right) on desktop, so it no longer leaves dead space.
  - **Readability + container:** non-compact `ProductCard` type bumped via `lg:` (price `text-sm`, name/location `text-xs`, specs `text-[11px]`, Call Now `text-sm`), grid gap `lg:gap-4`, container widened `lg:max-w-6xl → xl:max-w-7xl` on header + content (kept aligned), search bar capped `lg:max-w-2xl` (mirrors BuyerTopBar), bottom padding `pb-24 → lg:pb-12` (no bottom nav on desktop, which is already `lg:hidden`).
  - **Verified** (Playwright at 1440 / 1024 / 390px): desktop shows sticky sidebar + toolbar + 4-col grid + full-width Brand Picks; sidebar sticks cleanly under the header on scroll; Sort popover anchors correctly; mobile identical to before. `npm run build` clean. (A transient Vite overlay seen mid-session was the user's own dev server on :8080 recompiling an unrelated `Profile.tsx` edit — my dev server ran on :8081 and was cleaned up.)

- 2026-07-25: **Product detail page (buyer) — desktop widened to a two-column layout (media left / info right); mobile unchanged.** `src/pages/ProductDetail.tsx`. The page was a single `max-w-2xl` (672px) vertical stack, so on desktop it read as a narrow phone-width strip. Restructured the top of the page into a `lg:grid lg:grid-cols-2 lg:gap-8 lg:items-start` split, widened the outer container to `max-w-2xl lg:max-w-6xl` (aligns with the BuyerTopBar's own `lg:max-w-6xl`), and left the recommendation rails full-width below.
  - **Left column (media):** the `MediaCarousel` wrapper is `lg:sticky lg:top-20`, so the image stays pinned while the info column scrolls. Carousel height bumped `h-[380px] sm:h-[460px] → lg:h-[520px]` so it fills the taller desktop column.
  - **Right column (info):** breadcrumb/title/price, vendor card, tabs, and the Details/Reviews content all moved into a `lg:mt-0 space-y-3` column beside the media. "You might also like" grid widened `grid-cols-2 → lg:grid-cols-4`.
  - **Animation preserved:** the two structural wrappers (two-column top, recommendations block) are `motion.div`s carrying `variants={reduced ? {} : page} initial="hidden" animate="show"` so the existing Framer stagger still propagates through the new nesting (plain-div wrappers would have broken variant inheritance). All new layout is `lg:`-gated — mobile is byte-for-byte the original single-column stack.
  - **Verified** (Playwright at 1440px + 390px, mock product forced to render by temporarily bypassing the Supabase not-found guards, then reverted): desktop shows media left / info right filling the width; on scroll the media stays sticky while specs/description/manufacturing scroll past; mobile is unchanged. Build + typecheck clean (no new ProductDetail errors).

- 2026-07-21: **Trends + Sale pages (buyer) — Submit Requirement card interleaved every 5 rows, per-breakpoint, matching New Arrivals / Followings.** `src/pages/Trends.tsx` and `src/pages/Sale.tsx`. Both already had a single bottom card + `quickRfqOpen`/`QuickRfqModal`; added the interleaving into their main product grids (kept the bottom card as a guaranteed CTA).
  - **Trends** (fixed 2-col feed): built a `feedNodes` array over `feedProducts` inserting a card at every multiple of 10 — multiples of 20 get `col-span-full lg:max-w-4xl lg:mx-auto` (both breakpoints), the in-between multiples of 10 get `col-span-full lg:hidden` (mobile-only) — rendered in the one existing `grid-cols-2 lg:grid-cols-4` grid. Net: mobile every 10 products (5 mobile rows), desktop every 20 (5 desktop rows).
  - **Sale** (2-col/3-col toggle feed): same idea but intervals follow the toggle — `mobileInterval = cols*5` (10 or 15), `desktopInterval = cols*10` (20 or 30). Built `dealFeedNodes` preserving each deal's `motion.div` + `listItem` stagger wrapper; the requirement cards are plain `col-span-full` grid items inside the existing `motion.div` grid (they don't stagger, which is fine).
  - **Verified** (Playwright DOM analysis): Trends (28 feed products) → desktop card after 20 with position-10 hidden, mobile cards after 10 & 20. Sale (16 sale products) → mobile card after 10; desktop shows none yet because the sale catalogue is only 16 (< 20 needed for 5 desktop rows) but the position-10 mobile-only card is present and correctly `display:none` on desktop — identical formula to Trends' verified desktop-20 path. Sale's motion grid renders the col-span-full card cleanly. Build + typecheck clean. (Left the existing inline copies in Followings/NewArrivals as-is; did not refactor to a shared helper to keep scope to the two requested pages.)

- 2026-07-21: **New Arrivals page (buyer) — "Today's New In" requirement card re-interleaved to the same per-breakpoint 5-row cadence as Followings.** `src/pages/NewArrivals.tsx`. Previously the page rendered products in separate per-chunk grids with a `SubmitRequirementCard` after each chunk (every `desktopCols*3` products ≈ 3 desktop rows). Replaced the chunk loop with a single continuous grid + a `feedNodes` array that interleaves the card every 5 rows per breakpoint. Because this page has a 2-col/3-col view toggle, the intervals are derived from the active mode: `mobileInterval = cols*5` (10 or 15), `desktopInterval = desktopCols*5` (20 or 30). Cards at a multiple of `desktopInterval` render `col-span-full lg:max-w-4xl lg:mx-auto` (both breakpoints); the in-between `mobileInterval` multiples render `col-span-full lg:hidden` (mobile-only, collapse out of the desktop grid with no gap). A single grid (not per-chunk grids) is required so the `lg:hidden` card can drop out of the desktop flow cleanly. Removed the now-dead `productsPerChunk`/`chunks`; kept the `displayProducts` last-row padding. **Verified** (28 live products, Playwright DOM analysis): 2-col desktop → visible card after product 20 with the position-10 card `display:none`; 2-col mobile → cards after 10 and 20; 3-col toggle renders a clean 6-col grid (no interleaved card yet since 28 < 30 = 5 desktop rows, which is correct). Build + typecheck clean.

- 2026-07-21: **Followings page (buyer) — added the shared "Submit Requirement" card, then interleaved it every 5 rows with a per-breakpoint cadence.** `src/pages/Following.tsx` was the only buyer feed tab without the card. Imported `SubmitRequirementCard` + `QuickRfqModal`, added `quickRfqOpen` state, and kept one card at the very bottom (guaranteed CTA when a feed is short) wrapped `lg:max-w-4xl lg:mx-auto` like the other pages.
  - **Interleaved every 5 rows, different per breakpoint (per the user's spec: "5 rows in mobile = 10 products, 5 rows in desktop = 20 products, should be different for mobile and desktop").** New module-level `feedWithRequirements(products, onQuickRfq)` helper renders a grid's children with a requirement card inserted at every multiple of 10 products: cards at multiples of **20** get `col-span-full lg:max-w-4xl lg:mx-auto` (shown on both breakpoints), while the in-between multiples of **10** get `col-span-full lg:hidden` (mobile-only — they `display:none` out of the desktop grid, leaving no gap). Net result: **mobile sees a card every 10 products (every 5 mobile rows: 10, 20, 30…); desktop sees one every 20 products (every 5 desktop rows: 20, 40…)**. Never trails the final product (`n >= products.length` guard). Wired into the two real product feeds on the page — "Following New-In" and "Most Popular"; "Following Top Performing" (fixed 4-item highlight) is left alone.
  - **Verified** via Playwright DOM analysis on the Most Popular grid (28 real products): desktop → visible requirement card after product 20, with the position-10 card present but `display:none`; mobile → visible cards after products 10 and 20. Screenshotted both. Build + typecheck clean.

- 2026-07-21: **For You page (buyer) — fixed the ragged 2-of-4 product row on desktop + removed the redundant in-page search.** `src/pages/ForYou.tsx`.
  - **Ragged row fix.** The full-width insert blocks (requirement / recent-views) were landing after product #10 and #14 (`ROWS_PER_* × COLS_MOBILE`, i.e. multiples of the mobile 2-col count only). On the desktop 4-col grid that left the two products right before a block stranded as a half-row of 2. Replaced with `PRODUCTS_PER_REQUIREMENT_BOX = 12` and `PRODUCTS_PER_RECENT_VIEW = 16` — multiples of 4 (lcm of mobile 2 and desktop 4) — so the row before every inserted block is complete on both breakpoints (3 clean desktop rows before the requirement box, 4 before recent-views). Verified the clean rows by temporarily forcing the feed to 16 items, then reverted (`visible = filtered.slice(0, batches * 8)`).
  - **Removed the in-page search.** The page had its own "Search products, vendors…" input that duplicated the BuyerTopBar's main search. Removed the input (and the standalone filter-icon button); folded the sliders icon into a single "Edit Preferences" pill button and kept "Reset preferences" — so the header now carries just the two preference controls (change / reset) the user asked to keep. `search`/`setSearch` state left in place (now always empty, harmless; its `filtered` branch is inert). Applies on both desktop and mobile (the top-bar search covers both). `Search` icon import still used by the onboarding/filter Category & Location pickers.

- 2026-07-21: **For You page (buyer) — "Related To Recent Views" ad enlarged for desktop.** `src/pages/ForYou.tsx`. It had been centered at `lg:max-w-2xl` (a small block mid-grid with ~150px thumbnails). Changed the wrapper from `col-span-full lg:max-w-2xl lg:mx-auto` → plain `col-span-full` so the ad spans the full grid width on desktop, and its 4 thumbnails (`lg:grid-cols-4`) are now product-card-sized. Scaled the card up at `lg:` too: padding `p-3 → lg:p-4`, heading `text-sm → lg:text-lg`, thumbnail radius `rounded-xl → lg:rounded-2xl`, gap `gap-2.5 → lg:gap-5`, overlay name/price `text-[11px] → lg:text-sm`. Mobile unchanged (2×2 grid, full width). Verified at 1440px + 390px (temporarily forced the feed to 16 items to render the ad, which appears at product #14, then reverted the one-line change — confirmed reverted).

- 2026-07-21: **For You page (buyer) — interleaved "Post Your Requirement" box replaced with the shared `SubmitRequirementCard`, matching the other buyer pages.** `src/pages/ForYou.tsx`. The feed's every-5-rows requirement insert was a bespoke local `PostRequirementBox` component (a plain titled "Post Your Requirement" card, different visual language from the rest of the app). Swapped it for `<SubmitRequirementCard onQuickRfq={() => setQuickRfqOpen(true)} />` — the exact same "Looking for products?" card (coral corner accents + 2-column desktop layout: Submit Requirement CTA left, Quick RFQ / Create New Requirement / My Previous Quotes right) used on New Arrivals / Trends / Sale. Wrapper widened to `col-span-full lg:max-w-4xl lg:mx-auto` to match New Arrivals. Deleted the ~45-line `PostRequirementBox` definition and its now-unused icon imports (`Zap`, `ClipboardList`, `FileText`, `ChevronRight`). The bottom `SubmitRequirementCard` (plain variant) was already the shared component and left as-is. **Verified:** build + typecheck clean; captured the interleaved card in context at 1440px (temporarily forced the feed to 16 items since the real catalogue has ~8 and headless infinite-scroll won't advance the batch counter, then reverted that one-line change — confirmed reverted, `visible = filtered.slice(0, batches * 8)`).

- 2026-07-21: **For You page (buyer) — desktop layout optimization (used the `design-taste-frontend` skill; product-UI read, not landing-page).** `src/pages/ForYou.tsx`. The page was mobile-first and just stretched to `max-w-6xl` on desktop: a full-width stretched search field, Edit/Reset links pinned to opposite far edges, left-stuck insert cards, and a full-width giant "Submit Requirement" button. All fixes are `lg:`-gated so mobile is byte-for-byte unchanged (verified at 390px).
  - **Header → desktop toolbar.** Title + subtitle now sit left, with the search input + filter button grouped on the right at a constrained `lg:w-[420px]` (Edit Preferences / Reset preferences tucked underneath). On mobile it still stacks in the original single-column order. Title bumped to `lg:text-3xl`, Sparkles icon to `lg:w-6`.
  - **Chips + count row.** Active-preference chips and the "N products for you" count now share one row directly above the grid (chips left, count right via `lg:ml-auto`); stacked on mobile. Note: this moves the chips from just-below-subtitle to just-above-the-grid on mobile — grouped with the count as a filter summary (only visible when preferences are set; a skipped-onboarding session has none, so mobile looks identical).
  - **Insert blocks + bottom card centered on desktop.** The interleaved `PostRequirementBox` / `RecentViewsAd` (previously `col-span-full lg:max-w-md`, left-stuck with dead space) and the bottom `SubmitRequirementCard` (previously full-width) are now `lg:max-w-2xl lg:mx-auto` (grid-item auto-margin centering). Empty state → `lg:max-w-xl lg:mx-auto`. `RecentViewsAd` thumbnails go `grid-cols-2 lg:grid-cols-4` (a 4-across ad strip on desktop).
  - **Verification:** build + typecheck clean; Playwright at 1440px confirmed the toolbar header and the centered bottom card, and 390px confirmed mobile is unchanged. **Not visually captured:** the interleaved insert blocks only render after 10+ products in the feed (the real catalogue has ~8), and forcing the 48-item fallback pool + infinite-scroll under headless automation wouldn't advance the batch counter (a harness quirk, unrelated to these edits — the feed/infinite-scroll logic was not touched). They use the exact same centering wrapper as the verified bottom card. Onboarding steps left as-is (already centered `max-w-md`, fine on desktop).

- 2026-07-21: **Freelancers page (buyer) — category filter pills now draggable-to-scroll on desktop.** `src/pages/Freelancers.tsx`. The category row (`All Experts / Fashion Designing / … / UI/UX & Website Development`) is a `scrollbar-hide overflow-x-auto` slider whose content overflows even at `max-w-6xl` (measured scrollWidth 1216 > clientWidth 1104), so the rightmost chip was cut off with no way for a desktop mouse user to reach it. Wired the existing `useDragScroll` hook onto that row (same fix as the New Arrivals / Trends sliders). **User was asked** (wrap-to-show-all vs. keep-slider-draggable) and chose keep-it-a-slider. Scoped to just the category pills — the controls row (Location/Rating/Filters/Sort) above it was left untouched. Verified via build + Playwright real mouse drag (scrollLeft 0→112) and confirmed a plain click still selects a category (drag doesn't swallow it).

- 2026-07-21: **Buyer side-drawer menu — "Dashboard" renamed to "Home" + added a "Video Close-Ups" item.** `src/components/buyer/BuyerTopBar.tsx` `menuItems`. The first item's label changed from "Dashboard" to "Home" (href unchanged, still `/home/new-arrivals` — the buyer home feed). Added `{ label: "Video Close-Ups", href: "/video-closeups" }` right after "Browse Products" (that route already existed in `App.tsx` → `VideoCloseUpsPage`, which opens the full-screen reel viewer). Labels pass through `t()`, which falls back to English for keys not in the Hindi/Gujarati dictionaries, so no i18n bookkeeping needed. Verified via build + Playwright: drawer shows "Home" (no "Dashboard"), and clicking "Video Close-Ups" navigates to `/video-closeups`.

- 2026-07-21: **"Looking for New Brands?" carousel extracted into a shared component and added to the Following → View All page, which never had it.** In hindsight, the prior session's "where is this section, it was there originally" report was likely about *this* page (`/home/followings/view-all`, reached via the main Following page's "View All" link) lacking the carousel entirely — not (only) a stale dev server, since the user then explicitly asked to add it there.
  - **New `src/components/buyer/NewBrandsCarousel.tsx`** — moved verbatim out of `Following.tsx` (which had it as a page-local function) so it can be reused. `Following.tsx` now imports it instead of defining it; removed the now-unused `Plus` icon import there (its only use was inside the carousel).
  - **`FollowingViewAll.tsx`** — added `follow` to its `useFollowing()` destructure, added a `discoverable` memo (`brands.filter(b => !b.isHidden)`, same reasoning as the main Following page: includes already-followed brands so a just-followed slide can show "Following" instead of vanishing), and rendered `<NewBrandsCarousel brands={discoverable} onFollow={(b) => follow(b.id)} />` at the very top of the page content, above the search bar and the followed-brands list. Page's existing `max-w-2xl` (no desktop-specific width) left as-is — not asked to widen it for desktop.
  - Verified via build + Playwright at 1280px and 390px: carousel now appears at the top of View All on both breakpoints; the main Following page (mobile + desktop) still renders identically after the extraction.

- 2026-07-20/21: **Followings page (buyer) — Follow→Following toggle fix, plus "Your Followings" 2-column desktop layout (briefly reverted on a false alarm, then restored).** `src/pages/Following.tsx`. Verified via build + Playwright (1280px desktop, 390px mobile, plus an actual click-through of the Follow button) at each step.
  - **Fixed: clicking Follow on "Looking for New Brands?" made the card vanish instead of showing "Following."** The carousel's `brands` prop was the parent's `suggested` list — filtered to exclude anyone already followed — so the instant a follow succeeded, the next render dropped that brand from the array and the slide disappeared before the button could ever show a "Following" state. Fixed two ways together: (1) parent now passes a broader `discoverable` list (`brands.filter(b => !b.isHidden)`, i.e. includes already-followed brands) instead of the followed-excluding `suggested`; (2) `NewBrandsCarousel` now freezes which brand ids get a slide the first time each is seen not-yet-followed (`seenRef`/`slideIds`, only ever grows), and each slide's Follow/Following button reads the *live* `isFollowing` off that id — so following a brand flips its own button (check icon, muted style, "Following" label, no-op on click) without the slide leaving the carousel.
  - **"Your Followings" 2-column desktop split (currently in place).** `followed.length > 3` gates a `lg:hidden` single column (unchanged mobile/small-desktop behavior) vs. a `hidden lg:grid lg:grid-cols-2 lg:divide-x lg:divide-gray-100` two-column split (`slice(0,3)` / `slice(3,6)`, up to 6 total). Does not duplicate real brands to force a full 6 when there are fewer than 4 — stays single-column in that case.
  - **Note on the brief revert:** this was pulled out once already after the user reported "the entire section removed," which read at the time like a complaint about this layout change. It turned out to be an unrelated stale-dev-server/browser-cache issue (confirmed via `git status`: nothing from that session had been committed, and a fresh `tsc`/build were clean) — the user then explicitly asked to re-add the 2-column layout. Restored as originally implemented.

- 2026-07-20: **Fixed the Sale page's (buyer) "Submit Requirement" — was a bespoke, inconsistent one-off instead of the shared component.** `src/pages/Sale.tsx` had its own hand-built "Not finding your price? / Post a bulk requirement... / Post RFQ" card (different visuals, no Quick RFQ/Create Requirement/My Previous Quotes options) instead of reusing `SubmitRequirementCard` like New Arrivals, Trends, ForYou, and SearchResults already do. Replaced it with `<SubmitRequirementCard onQuickRfq={() => setQuickRfqOpen(true)} />` (`quickRfqOpen` + `QuickRfqModal` already existed on this page for the hero's Quick RFQ button, so no new state needed), wrapped the same `lg:max-w-4xl lg:mx-auto` way as the other pages. `Tag` icon import still used elsewhere on the page (Shop-by-Discount heading, empty-state icon) — left in place. Verified via build + Playwright at 1280px/390px.

- 2026-07-20: **Trends page (buyer) desktop polish — bigger/centered header, featured image traded for 3 smaller "look" cards, bigger brand avatars + more brands, Submit Requirement card.** `src/pages/Trends.tsx`. Verified via production build + Playwright (1280px desktop, 390px mobile diffed for regressions).
  - **"NEW TREND INSIGHTS" header + category chips, centered and bigger (desktop only)** — h1/subtitle bumped to `lg:text-4xl`/`lg:text-lg`. Category chips: split into an outer `overflow-x-auto` viewport + inner `flex lg:w-max lg:mx-auto` row (same centering pattern as the New Arrivals categories fix) so it centers when it fits instead of hugging the left edge; chip avatar/padding/text sized up at `lg:`. Wired the existing `useDragScroll` hook onto the chips rail for the same reason it was added to New Arrivals' rails. Mobile classes untouched.
  - **Featured category "hero" image is smaller + there are more of them (desktop only)** — was one `lg:aspect-[16/10]` image spanning the full container width. Now `grid-cols-1 lg:grid-cols-3`: the real featured card (now portrait `lg:aspect-[3/4]`, matching its mobile aspect instead of a wide banner) plus two more `hidden lg:block` "trending look" cards (same category, differently-seeded placeholder imagery, no save button — lighter-weight supplementary cards). Mobile renders only the single original full-width card, byte-for-byte as before.
  - **Top Brands avatars bigger on desktop + more brands per category** — avatar `w-14 h-14` → adds `lg:w-24 lg:h-24` (button/gap/badge/name text sized up alongside, all under `lg:`); every category's `brands` array extended to 6 entries (Cargo and Knitwear were down to 3, Long Dress/Short-sleeved-Tee to 4 — new entries follow each category's existing naming tone, reuse the `img()` picsum-seed helper already used throughout this file). Mobile avatar size (`w-14 h-14`) untouched.
  - **Submit Requirement card added, matching New Arrivals** — imported `SubmitRequirementCard` + `QuickRfqModal` (previously absent from this page), added `quickRfqOpen` state, rendered the card with `onQuickRfq` (the full two-column desktop variant with Quick RFQ / Create Requirement / My Previous Quotes) right after the infinite-scroll product feed's "Scroll for more" sentinel.

- 2026-07-19: **Fixed "Brand Picks" (and Video Close-Ups) rail not sliding for desktop mouse users on the buyer home feed.** Diagnosed empirically (Playwright) before touching anything: the rail genuinely had overflow content and a working `scrollLeft`, but a real mouse click-and-drag left it at `0`, and a plain vertical wheel doesn't pan a horizontal-only rail either — with `scrollbar-hide` removing the only other affordance, a desktop mouse user had no way to move it at all.
  - **New `src/hooks/useDragScroll.ts`** — click-and-drag horizontal scrolling for any `overflow-x-auto` rail: tracks pointer delta and sets `scrollLeft` directly, suspends `scroll-snap-type` for the duration of a drag (it was fighting a live JS-driven scroll and silently zeroing it out on the one rail using `snap-x snap-mandatory`), and swallows the click that follows a drag via `onClickCapture` so a dragged-over `Link`/button doesn't accidentally navigate/fire.
  - Wired into `NewArrivals.tsx`'s three horizontal rails: Brand Picks (the reported one), Video Close-Ups, and the "WHAT'S ON YOUR MIND" categories slider. Verified with real Playwright mouse-drag simulation (not just programmatic `scrollLeft`) that all three now move, and that a genuine (non-drag) click on a Call Now button or a card still behaves exactly as before (no accidental navigation, no broken clicks).

- 2026-07-19: **Buyer-home UI polish pass #2 (desktop) — categories centering, button text-align bug, sparse rails filled.** Follow-up to the polish pass below; same verification method (build + Playwright screenshots at 1280px/390px, mobile diffed for regressions).
  - **"WHAT'S ON YOUR MIND" categories now center when they fit the row** — `NewArrivals.tsx`: split the single scroll div into an outer `overflow-x-auto` viewport + an inner grid with `lg:w-max lg:mx-auto` (fixed `lg:auto-cols-[128px]` instead of a percentage, since percentage tracks can't resolve against an intrinsically-sized `w-max` box). Auto margins collapse to 0 if content ever exceeds the container, so scrolling still falls back correctly — deliberately avoided the classic `flex justify-center` + `overflow-x-auto` scroll-position bug. Mobile keeps its original `auto-cols-[22%]` edge-aligned layout untouched (no `lg:` prefix touches it).
  - **Fixed off-center "Submit Requirement" button text** — the two-column desktop layout from the prior pass put `lg:text-left` on an ancestor, which the full-width button's text silently inherited (it had no alignment of its own). Added explicit `text-center` directly on the button (`SubmitRequirementCard.tsx`) so it's centered regardless of the surrounding column's alignment.
  - **"Looking for these?" and "Brand Picks" no longer look sparse on wide screens** — both only had 3 mock items; `LOOKING_FOR_THESE` and `BRAND_PICKS` extended to 6 (reusing images already present elsewhere in the file, no new external URLs introduced). "Looking for these?" (`grid-cols-3 lg:grid-cols-6`) gates the extra 3 behind `hidden lg:block` per card so **mobile still renders exactly the original 3-item row**; desktop shows a full 6-across row. "Brand Picks" is a horizontal scroll rail, so the extra items simply extend what's reachable by scrolling — mobile's initially-visible cards are byte-for-byte unchanged, desktop's wider viewport now has enough cards to fill (with a slight peek of the next card, signaling scrollability rather than reading as empty).

- 2026-07-19: **Buyer-home + landing UI polish pass (desktop).** Four focused layout fixes, verified via production build + Playwright screenshots (desktop 1280px & mobile 390px).
  - **Landing logo shrunk** — `Landing.tsx` header `CosoraLogo` height 34→28 (mobile) / 40→32 (desktop). Footer logo untouched.
  - **"Call Now" CTAs now bottom-align across a row** — cards had ragged button positions when text wrapped (e.g. a two-line manufacturer/price). Made the card a `flex flex-col h-full`, the content block `flex flex-col flex-1`, and inserted a `flex-1` spacer before the button so every CTA pins to the card's bottom edge regardless of text height. Applied to both `NewArrivals.tsx`'s local `ProductCard` **and** the shared `components/buyer/ListingProductCard.tsx` (fixes For You / Trends / Following too).
  - **"Today's New In" grid fills complete desktop rows** — a live-product count like 6 left a half-empty row on `lg:grid-cols-4`. Added `desktopCols = cols*2`; a `displayProducts` useMemo pads the feed up to the next multiple of `desktopCols` by cycling existing products (unique `-fillN` ids for keys), and `productsPerChunk` is now `desktopCols*3` so every chunk (incl. the last, before each Submit-Requirement box) is edge-to-edge on desktop. `desktopCols` is a multiple of the mobile column count, so mobile stays full too.
  - **"Looking for products?" card optimised for desktop** — was `lg:max-w-md`, stuck to the left. Wrapper → `lg:max-w-4xl lg:mx-auto` (centered, wider); `SubmitRequirementCard.tsx` now renders a 2-column desktop layout **only** for the `onQuickRfq` variant (intro/CTA left, quick-action rows right with a left divider `lg:border-l`). Plain variant (ForYou bottom bar, SearchResults empty state) and all mobile layouts unchanged.

- 2026-07-19: **Closed the real `ad_location_scope` gap — enforcement in the paid ad-publish path (edge functions), not just the direct-insert trigger.** The prior `enforce_ad_location_scope` trigger only guarded direct authenticated inserts; the actual campaign publish runs as the service role in `razorpay-verify-payment` / `razorpay-webhook`, which the trigger's `current_user` guard bypasses — so the money-changing-hands path still trusted the client's `target_cities`. Now both functions resolve the vendor's **effective plan server-side** before inserting (read `vendor_subscriptions` + `subscription_plans` directly, since `get_vendor_plan` is self/admin-guarded and returns null for a service-role call on another vendor — same effective-plan rule: active + `current_period_end > now()`, else Free).
  - **Free (`ad_location_scope='none'`) → not published + flagged.** A payment that reaches publish for a Free vendor doesn't create an ad; the order is marked `ad_orders.status='refund_review'` (durable trace for manual refund/admin review — never a silent drop). Migration `20260719122000_ad_orders_refund_review_status.sql` extends the `ad_orders_status_check` lifecycle (`created/paid/failed`) with `refund_review` (kept distinct from `failed`, which is a gateway/publish failure owing no refund). Live/webhook flip the claimed order; demo mode (no intent row) records a `demo_blocked_<uuid>` trace row. Trace helpers now check the write succeeded so the response never claims a `refundFlagged` that didn't persist.
  - **Paid tier over its city allowance → clamped, still published.** `target_cities` is trimmed to the first N (`state_1`→1, `state_4`→4, `pan_india`/`global`→unlimited) and the ad publishes normally. Deliberate: ad pricing doesn't vary by city count, so clamping removes no paid-for line item; failing the whole order over a targeting detail would deny a vendor who paid. (`verify-payment` v5, `verify_jwt=true`; `webhook` v3, `verify_jwt=false`; both redeployed. Client `Advertisements.tsx` city UI, ad pricing, and the three prior triggers untouched.)
  - **Verified against the real deployed function** (signed in as `demo-vendor@cosora.dev`, plan temporarily flipped then restored to its exact Gold snapshot; all test rows cleaned up): Basic vendor, spec with 3 target cities → response `requested:3, allowed:1` and the **published** ad row has `target_cities:["mumbai"]` (clamped, not the requested 3). Effective-Free vendor → response `plan_not_eligible / refundFlagged:true`, **0 ads published**, and an `ad_orders` row `status='refund_review'` (amount ₹154, original 2-city spec captured). Advisors unaffected (CHECK-constraint change only, no new definer functions).

- 2026-07-19: **Server-side enforcement for the 3 remaining bypassable tier flags (`has_auto_catalog`, `ad_location_scope`, `leads_per_month`).** Follow-up to the `product_cap` fix; migration `20260719121000_enforce_plan_limits_catalogues_ads_quotes.sql`, applied live to `vxdhhgdfubqedfpwfyrb`. Each new trigger fn mirrors `enforce_product_cap()` exactly: SECURITY INVOKER + `current_user <> 'authenticated'` service-role/seed bypass, effective-plan resolution via `vendor_subscriptions` (active + `current_period_end > now()`, else `free`), `P0001` messages. Proven with impersonated `authenticated` inserts (all rolled back); `enforce_product_cap` left untouched; advisors clean (all three are SECURITY INVOKER — not in the definer lists).
  - **`has_auto_catalog` → `enforce_catalogue_plan()` / `trg_catalogues_plan` on `catalogues`.** Blocks a vendor whose effective plan lacks the feature (Free) from creating a catalogue (any INSERT) or transitioning a draft into a visible state; Basic+ unaffected. `createCatalogue` runs as the authenticated client, so this fully gates the real flow. **Tests:** Free INSERT → blocked (`P0001`); Basic (in-txn sub) INSERT → allowed; Gold INSERT → allowed; service-role INSERT → allowed (bypass).
  - **`ad_location_scope` → `enforce_ad_location_scope()` / `trg_ads_location_scope` on `advertisements`.** (a) Free (`scope none`) blocked from creating any ad; (b) paid tiers capped at `jsonb_array_length(target_cities)` ≤ allowance (`state_1`→1, `state_4`→4, `pan_india`/`global`→unlimited). UPDATE blocks only a location-count INCREASE beyond allowance, so same-scope edits (pause/budget/reducing cities) are never collaterally blocked. **Tests:** Free INSERT → blocked; Basic 2-location INSERT → blocked, exactly-1 → allowed; existing 2-city ad budget edit → allowed; increasing 2→3 cities → blocked; Gold 5-city → allowed. **Caveat (documented, matches the pattern):** the primary *active-campaign* publish path is the Razorpay verify/webhook edge function inserting via the service role, which — like seeds — the `current_user` guard intentionally bypasses; this trigger hardens the direct authenticated insert/update surface RLS exposes (any non-`active` ad). No buyer-geo delivery was built (still "rolling out").
  - **`leads_per_month` → `enforce_lead_cap()` / `trg_quotes_lead_cap` on `quotes`.** Blocks a new quote when the vendor's distinct-RFQ count for the current period ≥ `leads_per_month` (live count matching `get_vendor_plan.leads_used`; window = active sub's `current_period_start`, else calendar month). Re-quoting an already-quoted RFQ (the `submitQuote` upsert → ON CONFLICT UPDATE, detected via existing `(rfq_id,vendor_id)` row) and any UPDATE are exempt — editing never consumes a new lead. `subscription_usage` deliberately left unwired (live-count only). `submitQuote` runs authenticated, so this fully gates the real flow. **Tests** (Free cap 10, 11 synthetic RFQs in-txn): 10 distinct quotes → allowed (`leads_used`=10); 11th → blocked (`P0001`); at cap, upsert re-quote of an existing RFQ → allowed; at cap, plain UPDATE of an existing quote → allowed; service-role bypass confirmed.

- 2026-07-19: **Subscription tier-features audit (senior QA) + fixed the confirmed `product_cap` server-side bypass.** Went flag-by-flag through `subscription_plans.limits` (all 10 correctly seeded per tier) and classified each: (a) genuinely enforced, (b) exists-but-bypassable (client-only, no DB backing), (c) not built. Verified empirically against live data + impersonated `authenticated` sessions (all rolled back), not by reading code alone.
  - **FIX — `product_cap` now enforced server-side (migration `20260719120000_enforce_product_cap.sql`).** New `enforce_product_cap()` + `trg_products_cap` BEFORE INSERT/UPDATE trigger on `products` (SECURITY INVOKER, `current_user='authenticated'` service-role bypass like `enforce_products_moderation`). Counts the vendor's listed products (`under_review`+`live` — same set `get_vendor_plan` reports) against their **effective** plan's cap (mirrors the expiry rule; `-1`=unlimited). Also blocks the draft→`under_review` **laundering** path (UPDATE into the counted set), closing the edit-mode hole the client gate left open. Existing over-cap seed vendors are untouched (forward-looking, non-destructive). **Proven:** pre-fix, an impersonated Tirupur (Free, cap 2, 5 listed) INSERT past cap **succeeded** (5→6); post-fix it's blocked (`P0001`), exact boundary at cap 2 (2 ok, 3rd blocked), drafts still allowed, laundering blocked, Gold under-cap allowed, edits of existing live rows allowed, service-role/seed inserts unaffected. Security advisors: no new findings (my fn is SECURITY INVOKER). The client gate in `Upload.tsx` stays as UX; the DB is now the source of truth.
  - **Audit results (unchanged this pass unless noted):** `has_verified_badge` **(a)** — OR-logic intact (`trustSealFromParts` in products/rfqs/vendor mappers + `get_vendor_plan.trust_seal`). `search_boost_tier` **(a, client-side)** — real sort weight in `products.ts` (`fetchLiveProducts`/`fetchCatalogue`/`sortTrending` order by boost desc); confirmed live that Gold vendor's products rank above all boost-0 vendors. Plan badge on the seller sidebar **(a)** — real `useVendorPlan().plan.name` chip + live usage line. Category-matched premium leads **(a, client-side)** — `fetchOpenRfqs` flags+sorts RFQs matching a paid vendor's product categories. `leads_per_month` **(b)** — client-blocked in `OpenRfqLeads` off a live-computed `leads_used`, but no trigger on `quotes`; **`subscription_usage` table has 0 rows / is never written by anything**. `ad_location_scope` **(b)** — client-only city-target cap + Free-can't-advertise gate; no server validation (buyer geo-delivery honestly labeled "rolling out"). `has_auto_catalog` **(b)** — client blocks Free in `UploadCatalogue.tsx`, but **proven bypassable**: an impersonated Free vendor inserted a `live` catalogue directly. `has_crm` **(c)** — only a `plan.display` comparison-table row; no CRM surface exists. `has_international` **(c as a gate)** — no buyer-geography/visibility gating anywhere; exists only as an honest "International-ready" display chip. Lead access channel differentiation (Free=website … Gold/VIP=SMS) **(c)** — never built; only a comparison-table string, all tiers share the same RFQ pool. `has_dedicated_am` / `has_realtime_alerts` **(c, intentionally deferred)** — scaffold columns only (`account_manager_id`, `real_time_alerts_enabled`), shown as comparison rows; nothing fakes an assigned human or alert delivery ("Set Alerts" just links to `/leads`). No new work on AM/alerts/international/CRM/leads/ads per the task's explicit boundaries.

- 2026-07-18: **Buyer product-listing production pass — real stats, real recommendations, persisted preferences, hardened writes.**
  - **Duplicate `/browse` retired:** `App.tsx` `/browse` now `<Navigate to="/search" replace />` (removed the `BrowseProducts` import/route + fake data); the in-app `/browse` links in `Landing.tsx`, `Index.tsx`, `Register.tsx` repointed to `/search`, and `DashboardHeader.tsx` "New Arrivals" → `/home/new-arrivals`.
  - **Product stats made real (migration `product_view_enquiry_counters`):** SECURITY DEFINER RPCs `increment_product_view(p)` / `increment_product_enquiry(p)` (mirror the ad_impression/ad_click pattern, `where … status='live'`, granted anon+authenticated). `products.ts` gained `recordProductView`/`recordProductEnquiry`; `ProductDetail.tsx` bumps **views_count** once per session (sessionStorage-deduped, on open of a live product — not localStorage-only recordView) and **enquiries_count** on the Chat/message-vendor action. **sold_count left unwired and reported plainly** — no product-level "deal completed" event exists (quotes/rfqs carry no product_id, no orders table).
  - **Literal "Product name" fixed** on buyer cards in `NewArrivals.tsx` (×2) and `SearchResults.tsx` → the real `{product.name}`/`{item.name}`/`{p.name}`.
  - **Real 404, byte-for-byte identical:** `ProductDetail.tsx` gained a static `ProductNotFound` + `isPending` spinner + `isError||!row` returns. A nonexistent id and an RLS-blocked (non-live, not-owned) id both resolve to `null` via `maybeSingle`, so both render the exact same not-found page — no existence oracle.
  - **Write hardening:** new `assertWrote(rows, action)` guard in `products.ts`; `deleteProduct`/`updateProduct` now `.select("id")` and throw on a silent zero-row write (RLS no-op no longer looks like success).
  - **Recommendations are real (rule-based, no AI):** buyer preferences now persist account-tied to a new **`buyer_profiles.preferred_categories text[]`** (migration `buyer_preferred_categories`). New `src/lib/buyerCategories.ts` holds the shared buyer categories + `PREF_TO_DB_CATEGORY_NAMES` mapping; `preferencesStore.ts` reads/writes the DB column (`dbSaveCategories`/`setPreferencesUser`, hydrated on sign-in via `StoreSync`, localStorage only when signed out); `ForYou.tsx` and `InterestPreference.tsx` share the same source. **"You might also like"** = products in the buyer's preferred categories ranked `enquiries_count` desc, `views_count` tiebreak (fallback to the current product's category), `useYouMightLike`. **"Brand Picks"** = the same vendor's other live products, `useVendorOtherProducts`. Both `live`-only, exclude the current product, and **hide when empty** (no padding). Removed the mock `brandPicks`/`youMightLike` fixtures.
  - **InterestPreference edit path fixed:** the shared screen now branches Save + back by route — `/auth/interest-preference` → `/auth/terms` (registration), `/profile/interest-preference` → back to `/profile` (edit); `Profile.tsx` Interest & Preferences "Edit" now routes to `/profile/interest-preference` (real entry point). Verified there is **no cold-load hydration race** in the pre-fill (the `useEffect([prefs.categories])` re-syncs reactively and `set()` always yields a fresh categories reference) — reported as already-correct, no change made.
  - `database.types.ts` updated (new RPCs + `preferred_categories`). `npx tsc --noEmit --skipLibCheck` clean after each task.

- 2026-07-17: **Admin RBAC (follow-up) — role-gated the remaining whole-row admin write policies.** Migration `20260717130100_role_gate_products_ads_vendorprofiles_writes.sql`. Three tables still had plain `is_admin()`-gated writes; brought them into line with the subscription-tables pattern by role-checking only the admin branch of each OR (owner conditions preserved verbatim): `products` insert/update/delete → `product_moderator`, `advertisements` insert/update/delete → `ads_moderator` (INSERT keeps its `status<>'active'` owner guard), `vendor_profiles` `vprofiles_write` (ALL) → `vendor_ops`. Reads untouched. Verified live (rolled back) with a `support`-role admin acting on rows owned by other vendors: INSERT → RLS violation, UPDATE/DELETE → 0 rows; the matching role → 1 row (proving it's the gate, not a missing row). This layers on top of the column-level triggers from the first migration.

- 2026-07-17: **Admin RBAC — fine-grained admin roles + real role-gated write enforcement (DB layer for the admin panel).** Migration `20260717130000_admin_roles_and_role_gated_rls.sql`, applied live to `vxdhhgdfubqedfpwfyrb`. This is Prompt 1 of 2; the admin panel is a separate repo that's only UI on top of this. `is_admin()`/`profiles.is_admin` are unchanged ("is this an admin at all"); this adds a second layer — *which* admin role.
  - **Schema:** enum `admin_role_type` (super_admin/product_moderator/vendor_ops/ads_moderator/finance_admin/support), nullable `profiles.admin_role` (null = admin with no role yet → fails closed), `admin_role()` (SECURITY DEFINER, mirrors `is_admin()`), and `vendor_profiles.account_status` (`active`/`suspended`, for the panel's vendor-suspension feature).
  - **Enforcement in 3 places (reads stay plain `is_admin()` — role-gating is writes only):** (1) **RLS** — `vendor_subscriptions`/`subscription_invoices` admin writes require `finance_admin` (added a `subscription_invoices` admin write policy that didn't exist). (2) **`BEFORE` triggers** (SECURITY INVOKER; RLS can't see *which column* changed) — products.status→live/rejected needs `product_moderator` (owner keeps draft↔under_review), vendor_profiles.is_verified/account_status needs `vendor_ops`, ad status changes on an ad you don't own need `ads_moderator` (vendors keep pause/resume of their own), and the **keystone**: only `super_admin` may change `is_admin`/`admin_role` (closes the pre-existing self-promotion hole). Triggers bypass for service-role/SECURITY DEFINER contexts via a `current_user='authenticated'` guard, so edge functions and the approve RPC are unaffected. (3) **`approve_vendor_content` RPC** (SECURITY DEFINER → bypasses RLS) tightened from "any admin" to `product_moderator`.
  - **Bootstrap:** seed admin `demo-admin@cosora.dev` promoted to `super_admin` (else role-gating deadlocks). Further super_admins are granted out-of-band (service role / SQL).
  - **Verified live** (impersonating `authenticated` with JWT claims, all rolled back): non-admin self-promote → blocked; vendor self-suspend → blocked; `support`-role admin doing a `vendor_ops` action → blocked; `super_admin` granting a role → succeeds. Security advisor clean apart from the same SECURITY-DEFINER-executable note the pre-existing `is_admin()` already carries.

- 2026-07-17: **Advertise feature — fixed the fake/decorative parts (targeting, trust-seal ads, competitor intel, old-ads, pricing catalog).** The real Razorpay create→verify→webhook flow, `advertisements` writes, My-Ads list, and buyer-side `active_ads`/SponsoredRail were kept as-is.
  - **Targeting is now real (was 100% decorative).** New `advertisements.target_categories` / `target_cities` (jsonb) threaded end-to-end: `buildSpec` → `AdSpec` → `adRows()` in razorpay-verify-payment + webhook → the row (migration `ad_targeting`). **Category** targeting genuinely filters buyer-side: `active_ads(max, filter_category)` returns untargeted ads (run everywhere) + ads targeting that category; wired on ProductDetail via the product's real `category_id` (new category-filtered SponsoredRail). Category picker now uses the vendor's **real DB categories** (`useVendorCategories`). **Gender** targeting was removed (no buyer gender signal, no plan tie — decorative). **City** targeting is plan-scope-limited (Part 5) + persisted, but honestly labelled "geo-based delivery rolling out" (no buyer location signal exists to filter on).
  - **Goals do something real:** selecting a campaign goal pre-selects the placement types that serve it (`GOAL_PLACEMENTS`), instead of being an inert picker.
  - **Trust-seal ad types now grant something:** buying `trustedSeal`/`verifiedCertificate` writes a time-bound `vendor_ad_verifications` row (expires with the campaign) + denormalised `vendor_profiles.ad_verified_until`, granted server-side in the same publish path (`grantSeals` in verify-payment + webhook, migration `ad_verifications`). Buyer trust seal is now `is_verified OR active plan OR active ad-verification` (extended `trustSealFromParts`, threaded through products/vendor/rfq mappers). The buy flow shows "Included in your {plan}" and blocks re-purchase when the vendor's plan already grants the seal (no double-charge).
  - **Pricing catalogs reconciled:** deleted the invented `ADVERTISE_CARDS` (different names/prices than the real catalog); the "Popular ad types" strip is now generated from real `AD_TYPES`/`AD_PRICE` — every displayed price is actually purchasable.
  - **CompetitorAds** (was fully fabricated named competitors) → real anonymized **category benchmarks** from `ad_category_benchmarks` RPC (per-category product/vendor counts, avg price/views, your-vs-peer reviews & photos) computed from actual data, with a "no individual competitor identified" disclaimer.
  - **OldAdvertisements** (was mock) → the vendor's real campaign history via `useMyAds` (effective status: active campaigns past `ends_at` read as Ended), with real Views/Clicks/CTR/budget and working Pause/Resume (`updateAdStatus`).
  - Verified live: create-order→`not_configured`→demo publish persists targeting + grants the seal; `active_ads` category filter confirmed (targeted ad shows only in its category, untargeted always). Live signature path shares `publishOrder`/`adRows`/`grantSeals` with the tested demo path. `npx tsc --noEmit` clean.

- 2026-07-17: **Subscription QC pass — closed a VIP self-activation hole and made 3 previously-inert tier features real.**
  - **Bug fixed (security):** VIP (invite-only) was self-activatable in demo/unconfigured mode — `subscription-create-order` returns `not_configured` (keys unset) *before* it can return `invite_only`, so the client fell through to demo activation. Now `subscription-verify-payment`'s shared `activateSubscription` **refuses any invite-only plan** server-side (redeployed v2). Verified: demo-activate VIP → `{ok:false,error:"invite_only"}`.
  - **Search result position — now a real ranking weight** (was stored-only): `products.ts` resolves each vendor's active-plan `search_boost_tier` (cached plans map) onto `ProductCardData.boost`/`CatalogueRow.boost`; `fetchLiveProducts`, `fetchCatalogue`, and `sortTrending` now rank higher tiers first (created_at/score as tiebreak). Genuine sort input, not simulated.
  - **Automatic catalog upload — now gated** (`has_auto_catalog`): `UploadCatalogue.tsx` blocks Free vendors from publishing PDF catalogues (banner + `handleSubmit` guard → upsell), matching the spec's Free=Manual.
  - **International buyer access — surfaced** (`has_international`): `vendor.ts` exposes `international` on the buyer vendor profile; `VendorProfile.tsx` shows a real "International-ready" chip for active Gold/VIP (replaced the hardcoded "| Exporter"). Note: true international-buyer *routing* still needs an international buyer segment that doesn't exist yet — this makes the tier benefit visible/honest, not fake delivery.
  - Verified live across all tiers (boost 0→4, auto-catalog false only for Free, international only Gold/VIP); `npx tsc --noEmit` clean.

- 2026-07-17: **Vendor subscription system — 5 tiers, real Razorpay billing, and limits enforced at the point of action.** Greenfield: first tracked SQL migrations in the repo (`supabase/migrations/`), applied live to project `vxdhhgdfubqedfpwfyrb`.
  - **Schema (migrations `subscription_tables` → `subscription_hardening`):** `subscription_plans` (text-slug PK free/basic/silver/gold/vip; prices, `limits` jsonb + `display` jsonb — every spec row modelled as data, no scattered booleans), `vendor_subscriptions` (one row/vendor, `status`/`billing_cycle`/`current_period_*`/`auto_renew` reminder-only + scaffold `account_manager_id`/`real_time_alerts_enabled`), `subscription_usage`, `subscription_invoices` (amount/gst/gstin/tds/invoice_number/period/pdf_url), `subscription_payment_orders` (mirrors `ad_orders`: created→paid, idempotent). Added denormalised `vendor_profiles.plan_id` + `plan_expires_at` cache. RLS: owner/admin **read** only; all writes via edge functions (service role). Seeded 5 tiers (yearly = 10× monthly ≈ 17% off; trust seal on **every paid tier** per override; no matchmaking-AI field) + demo-vendor on active Gold.
  - **RPC `get_vendor_plan(v)`** (SECURITY DEFINER, self/admin-guarded): resolves the **effective** plan (falls back to Free on read when a period lapses — the expiry rule), with limits + **live** usage (products_used = under_review+live count; leads_used = distinct RFQs quoted this period). Single read model for `useVendorPlan`. Also `next_invoice_number()` + `expire_subscriptions()` sweeper (service-role only).
  - **Edge functions (deployed):** `subscription-create-order` / `subscription-verify-payment` / `subscription-webhook` — modelled on the ad functions. Amount computed server-side from the plan (+18% GST); demo/simulated activation when `RAZORPAY_*` unset (verified: `not_configured` → demo activate → plan flips → invoice `INV-2026-000001` with GST ₹126 on ₹699). Verify/webhook activate idempotently via the created→paid claim.
  - **Client:** `src/lib/plan.ts` (pure model/helpers: limits, `trustSealFromParts`, tier styles, ad-scope), `src/lib/queries/subscriptions.ts` (`useSubscriptionPlans`/`useVendorPlan`/`useVendorInvoices` + `purchaseSubscription` orchestration). Rebuilt `Subscription.tsx` on live data (5 tiers, live usage bars, data-driven comparison table, GSTIN/PAN persisted to profile+invoice, live billing history) + new printable `InvoiceDetail` page (`/subscription/invoice/:id`, browser print-to-PDF).
  - **Enforcement at point of action:** `Upload.tsx` blocks publishing beyond `product_cap` (drafts/edits allowed); `OpenRfqLeads` blocks quoting past the monthly lead cap + shows remaining; `Advertisements.tsx` gates Free from ads and caps location targets to the tier's `ad_location_scope`. **Trust seal** now derived everywhere buyers see it (`is_verified OR active paid plan`) via the query mappers — never overwrites the admin `is_verified` column. **Category-matched premium leads** (rule-based): paid vendors get RFQs matching their product categories flagged + sorted first. Sidebar shows the vendor's plan chip + usage.
  - **Config still needed for live payments:** `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `RAZORPAY_WEBHOOK_SECRET` (Supabase → Edge Functions → Secrets; webhook → `/functions/v1/subscription-webhook`, event `payment.captured`). Scaffolded-not-real: account-manager assignment, WhatsApp/SMS alerts, sales concierge, search-ranking boost (stored as `search_boost_tier`, not yet wired into buyer ranking). `npx tsc --noEmit` clean.

- 2026-07-05: **Vendor production pass (part 2) — Leads + Quotes consolidated onto the real RFQ→quote loop; BusinessProfile ratings real.**
  - **Leads page rewritten** (was ~1,250 lines of mock): now a lean page around the real `OpenRfqLeads` (live buyer-RFQ pool with inline quoting) + a proper empty state. Deleted the fabricated `LEADS` browse feed (fake "Rajesh Kumar" buyers), `LeadCard`, `LeadsInsights`, and the dead category/search filters.
  - **Quotes page restructured**: dropped the mock "Requests" tab (fake `234 Open RFQs` stats, `RFQ_DATA` cards, filters, `RFQDetailPage`) and the tab switcher. The page is now real end-to-end — live requests via `OpenRfqLeads` + the vendor's real submitted quotes (`useMySubmittedQuotes`) with real stats + empty state.
  - **BusinessProfile**: the Reviews & Ratings block (score, star row, breakdown bars, reviewer count) now reads the vendor's real `useVendorReviews` data (was a hardcoded 4.5 / 20 users / fixed bars). Product/video galleries on this **self-preview** page remain demo (buyers see the real `/vendor/:id`, already fully wired) — flagged for a follow-up that extends `useMyProducts` with moq/fabric/gsm/fit.
  - **Backend:** re-verified RLS complete on all tables; performance advisories are standard at-scale hints (unindexed FKs, `auth.uid()` init-plan, multiple-permissive-policies) applying project-wide, no correctness/security issues. Build green.

- 2026-07-05: **Vendor production pass (part 1) — SellerHome + Analytics de-mocked, backend verified.**
  - **SellerHome dashboard:** the three hardcoded fixtures are now real — **Top Performing Products** (`useMyProducts`, live products by inquiries, real views), **Recent Leads** (`useOpenRfqs` — real active buyer RFQs with a "Send a quote"/"View quote" CTA → `/leads`, replacing the fake Rajesh/Priya buyers + fake Call/Message buttons), and **Recent Messages** (`useConversations` — real threads, each opening `/chats/:id`). All have empty states.
  - **`useMyProducts`** now selects `views_count` (was hardcoded `views: 0`), so product view counts are real across Products/SellerHome/Analytics.
  - **Analytics:** KPI cards (Total Views, Inquiries, Conversion, Active Products), the Top-Performing-Products table, and the "AI insight" line are now derived from the vendor's real products + `useVendorDashboard` (were fully hardcoded). Added an honest caption that the **trend charts remain illustrative** until visit-level event tracking exists (we don't yet collect per-day views/sources).
  - **Quotes → "My Quotes" tab:** now shows the vendor's **real** submitted quotes via new `useMySubmittedQuotes(vendorId)` (`quotes` joined to `rfqs`, status mapped pending→awaiting / shortlisted→in_negotiation / accepted / rejected→not_selected), with real Total/Accepted/In-Negotiation/Pending stats and an empty state. Replaced the `SUBMITTED_QUOTES` fixture. (The "Browse RFQs" tab's `RFQ_DATA` list is still demo, but the real `OpenRfqLeads` browse+quote panel already sits above it.)
  - **Backend check:** verified every `public` table has RLS enabled AND has policies (no table without RLS, none locked-out); build clean.
  - **Still mock (flagged for the next pass):** `Leads.tsx` mock `LEADS` browse feed + `LeadsInsights` (real `OpenRfqLeads` panel already sits above it); `Quotes.tsx` `RFQ_DATA` browse tab; `BusinessProfile.tsx` office photos / product grid / employees; `Subscription` current-plan; `CompetitorAds`/`MyBusiness`/`BusinessTools` demo content.

- 2026-07-05: **Razorpay hardening — webhook backstop, idempotent publish, and ad-creation RLS lockdown.** Follow-up making the ad payments production-safe.
  - **Payment intents (migration `ad_payment_intents_and_rls_lockdown`):** new `ad_orders` table (`order_id` pk, `vendor_id`, `spec` jsonb, `amount`, `status` created→paid, RLS: owner-read only, no client writes). `razorpay-create-order` now records the full spec + amount here (server-side) and returns just `{orderId, amount, keyId}`. `razorpay-verify-payment` (live) verifies the signature then **publishes by claiming the intent** (`UPDATE … status='created'→'paid' RETURNING`, then inserts the ads) — so it and the webhook are **idempotent** and never double-publish; spec/vendor come from the stored intent, not the client.
  - **Webhook (`razorpay-webhook`, `verify_jwt=false`):** server-to-server backstop — verifies the `x-razorpay-signature` HMAC of the raw body (`RAZORPAY_WEBHOOK_SECRET`) and publishes the same intent on `payment.captured`/`order.paid`, so a closed browser mid-redirect still gets the campaigns. Deployed + reachable (`not_configured` until the secret is set).
  - **RLS lockdown:** replaced the `advertisements_write` ALL policy with insert/update/delete policies — clients **cannot insert an `active` ad** (`status <> 'active'` check) and a `guard_ad_activation` BEFORE-UPDATE trigger blocks laundering a draft into active (activation only from active/paused, or service role/admin). So the only path to an active ad is a server-verified payment. Verified in SQL: active-insert blocked, draft→active blocked, pause/resume still works.
  - **Demo path now server-side too:** the no-keys fallback calls `razorpay-verify-payment` with `{demo:true, spec}` (new `publishDemoAds` in `payments.ts`) — the edge function creates the ads, so the client never inserts ads directly even in demo. Removed the client `createAd` path from the flow.
  - **Extra secret for the webhook:** `RAZORPAY_WEBHOOK_SECRET` (in addition to `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`); set the same value as the webhook's signing secret in the Razorpay dashboard, pointing it at `/functions/v1/razorpay-webhook`. Types updated (`ad_orders`), `npm run build` clean.

- 2026-07-05: **Real Razorpay gateway wired for ad purchases (server-verified), with a graceful fallback until keys are set.** Replaces the mock-only checkout with a production payment flow.
  - **Edge functions (deployed, `verify_jwt` on):** `supabase/functions/razorpay-create-order` computes the amount **server-side** from the ad spec (mirrors AD_TYPES pricing — never trusts a client amount), creates a Razorpay order via the Orders API (Basic auth with `RAZORPAY_KEY_ID:RAZORPAY_KEY_SECRET`), and returns `{orderId, amount, keyId}`. `razorpay-verify-payment` recomputes the HMAC-SHA256 signature (`orderId|paymentId`), and only on a match **creates the campaigns server-side** via the service role — one active `advertisements` row per promoted product — with the **vendor id taken from the JWT** (not the client body). Both return `{error:"not_configured"}` until the secrets exist.
  - **Client (`src/lib/queries/payments.ts`):** `createRazorpayOrder` / `openRazorpayCheckout` (dynamically loads `checkout.js`, coral theme, resolves on success / rejects `"dismissed"`) / `verifyRazorpayPayment`. `Advertisements.tsx` "Pay & Publish" now: create order → if configured, open **real Razorpay Checkout** → verify → ads publish (server-side); if **not configured**, opens the existing simulated modal (now labelled "Demo checkout — no real charge") and publishes client-side. Verified deployed + reachable: both functions return `not_configured` with the anon JWT, so the fallback path is active today.
  - **To go live:** set `RAZORPAY_KEY_ID` + `RAZORPAY_KEY_SECRET` in Supabase → Edge Functions → Secrets. No redeploy/code change needed — the flow switches to real Razorpay automatically. (Optional hardening later: tighten `advertisements` INSERT RLS so only the verify function can create `active` ads.)

- 2026-07-05: **Functional advertisements — select type → pay → publish to the buyer feed → track analytics.** The ad builder created a bare row and nothing reached buyers; now it's an end-to-end loop.
  - **Schema (migration `advertisements_buyer_publish_and_analytics`):** buyers read active ads through a SECURITY DEFINER RPC **`active_ads(max_count)`** that joins `advertisements → products (live) → vendor_profiles` and returns only safe fields (product name/image/price/vendor — never budgets). Impressions/clicks bump via **`ad_impression(ad)`** / **`ad_click(ad)`** RPCs (all three intentionally granted to `anon`+`authenticated` so buyers — signed in or not — can view/track). `createAd` gained `endsAt`.
  - **Vendor create flow (`Advertisements.tsx`):** Step 2 now selects the vendor's **real live products** (`useMyProducts`, image tiles, stores product UUIDs) instead of placeholder icons. "Create Your Ad" became **Pay & Publish** → a **PaymentModal** (UPI/Card mock checkout, simulated gateway latency) that on success creates **one active campaign per selected product** (with `product_id`, product image, `daily_budget`, `placement`, `ends_at`). Empty-state links to `/upload` when the vendor has no products. (No real gateway — a Razorpay/Stripe integration would swap into PaymentModal + activate on webhook.)
  - **Buyer publishing:** new `src/components/buyer/SponsoredRail.tsx` (`useActiveAds`) renders a **"Sponsored"** rail of promoted product cards on the buyer home (`NewArrivals`, above Video Close-Ups). It logs an impression per ad on view and, on tap, logs a click and opens `/product/:id`.
  - **Analytics:** `queries/ads.ts` gained `useActiveAds` + `logAdImpression`/`logAdClick`. The vendor's **"Your Campaigns"** now shows a totals strip (Active · Impressions · Clicks · CTR), a per-row CTR, and each campaign's product thumbnail.
  - **Seed:** two active demo campaigns (Demo Textiles Co. + Tirupur Textiles, with impressions/clicks) so the Sponsored rail and analytics render immediately. Types updated, advisors clean (baseline SECURITY-DEFINER WARNs only), `npm run build` clean.

- 2026-07-05: **Service reviews connected — service vendors & freelancers now take real reviews.** The service/freelancer profile "Write a Review" only toasted; now it persists and the My Reviews "Service" filter is real.
  - **Schema (migration `service_reviews`):** service vendors/freelancers/photographers are still client-side mock (no DB rows), so `service_reviews` keys off the mock seed id + `service_kind` (no FK) and denormalizes `service_name`/`reviewer_name`. Public read; buyer-owned writes; one review per buyer per entity (upsert). No aggregate trigger (nothing to sync — the client computes avg/count, falling back to the mock rating until real reviews exist).
  - **Query layer:** `queries/reviews.ts` gained `useServiceReviews(kind, id)` and `submitServiceReview`/`updateServiceReview`/`removeServiceReview`; `useMyReviews` now merges vendor + product + **service** into one `MyReviewItem[]` and `MyReviews.tsx` routes edit/delete by type.
  - **UI:** `ServiceVendorProfile` + `FreelancerProfile` reviews tabs show real avg/count/list (fallback to mock) and their Write-a-Review modals persist. `PhotographerProfile` has no review section to connect (left as-is; the table already supports the `photographer` kind for the future).

- 2026-07-05: **Product reviews connected too — ProductDetail ratings + the My Reviews "Product" filter are now real.** Follow-up to the vendor-review loop; the product review tab was still mock and only `toast`ed.
  - **Schema (migration `product_reviews_buyer_to_product`):** new **`product_reviews`** table mirroring `reviews` but targeting a product (`product_id → products`, `buyer_id → auth.users`, `rating 1-5`, `body`, `reviewer_name`, optional `size_bought`/`photos[]`, **unique (product_id, buyer_id)**). RLS identical shape (public SELECT; writes scoped to `buyer_id = auth.uid()`; admin delete). Trigger **`sync_product_rating`** keeps `products.rating_avg`/`reviews_count` in sync; reuses the hardened `touch_reviews_updated_at`. Verified anon read OK + anon insert RLS-blocked. Regenerated `database.types.ts`.
  - **Query layer:** extended `src/lib/queries/reviews.ts` — `useProductReviews(productId)` (list + avg + count + breakdown), and `useReviewMutations()` gained `submitProductReview`/`updateProductReview`/`removeProductReview` (shared reviewer-name resolver). `useMyReviews()` now **merges** vendor + product reviews into one `MyReviewItem[]` (with a `kind` discriminator), sorted by date.
  - **Buyer ProductDetail (`/product/:id`):** the Reviews tab now shows the product's **real** average, count, star breakdown and customer-review list (name, stars, date, body, size, photos) for live products; Write-a-Review actually submits (create/edit, saving state). Falls back to the mock template for non-DB demo products. Empty state reworded.
  - **Buyer My Reviews (`/profile/reviews`):** now lists both vendor and product reviews; the Vendor/Product filter chips are both real, and Edit/Delete route to the correct table by type.
  - **Seed:** 11 demo product reviews across the 3 newest live products (Premium Cotton Polo, Oversized Crew Tee, Linen Camp Shirt) so the tab is populated. `npm run build` clean.

- 2026-07-05: **Reviews connected end-to-end (buyer ⇄ vendor), fully DB-backed.** Buyer reviews used to just `toast` and both review pages were mock; now a buyer's review reaches the vendor and drives the vendor's real rating.
  - **Schema (migration `reviews_buyer_to_vendor` + `reviews_harden_functions`):** new **`reviews`** table (`vendor_id → vendor_profiles`, `buyer_id → auth.users`, `rating 1-5`, `body`, denormalized `reviewer_name`/`reviewer_company`, `reply_body`/`replied_at`, `created_at`/`updated_at`, **unique (vendor_id, buyer_id)** so editing is an upsert). RLS: **public SELECT** (buyers + anon browsers + the vendor all read), INSERT/UPDATE/DELETE scoped to `buyer_id = auth.uid()` (+ admin delete). A trigger **`sync_vendor_rating`** keeps `vendor_profiles.rating_avg`/`reviews_count` in sync on every insert/update/delete; a SECURITY DEFINER RPC **`reply_to_review`** lets a vendor add/edit only the reply fields on reviews about them (never the buyer's rating/body). Verified: anon read OK, anon insert RLS-blocked, aggregate trigger correct; hardened `search_path` + revoked RPC execute on the trigger fns (advisors clean apart from the pre-existing baseline). Regenerated `database.types.ts`.
  - **Query layer:** new `src/lib/queries/reviews.ts` — `useVendorReviews(vendorId)` (list + avg + count + 5→1 breakdown), `useMyReviews()` (buyer's own, joined to vendor identity), and `useReviewMutations()` → `submit` (upsert, auto-fills reviewer name/company from `buyer_profiles`/`profiles`), `updateReview`, `removeReview`, `reply` (RPC). All invalidate `["reviews"]` + `["vendor_profile"]`.
  - **Buyer VendorProfile (`/vendor/:id`):** Write-a-Review modal now really submits (create/edit, with saving state); the Reviews & Ratings block shows the **real** average, count, star breakdown, and a live review list (reviewer, stars, date, body, plus the vendor's reply) with a View-all toggle. Falls back to the `vendor_profiles` aggregate until a vendor has real reviews.
  - **Vendor Reviews page (`/reviews`):** reads the signed-in vendor's real reviews (rating, breakdown, count) and **Reply** now posts through the RPC and persists (was local-only). Mock array removed.
  - **Buyer My Reviews (`/profile/reviews`):** lists the buyer's real vendor reviews; Edit and Delete now hit the DB. Seed array removed. (Product/Service review types remain as future filters.)
  - **Seed:** added 9 realistic demo reviews across Demo Textiles Co. (4.4★ ×5) and Tirupur Textiles (4.8★ ×4), two with vendor replies, so the loop is visible immediately. `npm run build` clean.

- 2026-07-05: **Vendor→buyer reflection audit — surfaced two vendor features that weren't reaching buyers.** Went through the vendor side vs the buyer surfaces; all routed links resolve (no dead routes), but two vendor-managed things were invisible to buyers on the buyer `VendorProfile` (`/vendor/:id`). Fixed both in `src/pages/VendorProfile.tsx` + `src/lib/queries/vendor.ts` (design-taste-frontend, redesign-preserve: matched the page's coral `#ef4d62`, `rounded-2xl` cards, and existing Framer variants; no em-dashes).
  - **Catalogues had no buyer surface.** Vendors upload PDF lookbooks (`/upload-catalogue` → `catalogues`, moderated to `live`) and a buyer-facing hook `useVendorCatalogues` already existed but was **rendered nowhere**. Added a **"Catalogues & Lookbooks"** section (only shows when the vendor has live catalogues): 2-col cards with cover image (or a tinted PDF placeholder when no cover), page-count badge, title/description, and a "View catalogue" link opening `fileUrl` in a new tab.
  - **Real vendor identity/contact wasn't reflecting.** The buyer `useVendorProfile` query fetched only a handful of columns and the page showed **hardcoded placeholders** ("Mr. K.S. Tomar", "contact@caramel.in", "www.caramel.in", "+91 90110 60851", a static banner) even though the vendor manages all of it at `/my-store`. Extended the query to select `logo_url, banner_url, phone, whatsapp, website, owner_name, owner_email, address_line, area, state, postal_code, landmark, gstin, pan` and wired them into the hero **banner**, **Contact Details** (owner/address/phone/email/website), **Detailed Information** (business type, MD, GST, PAN) and the **Website** link (now a real `target=_blank` link) — each falling back to the demo value when a field is empty so mid-onboarding vendors still render a complete card. Also passed the real `brandName` into the Write-a-Review modal and the All-Products save action (were hardcoded to "CARAMEL"/`BRAND`).
  - Verified: `npm run build` clean (the only `tsc` VendorProfile notes are the pre-existing `openSaveModal reviews: string` mismatch, unchanged by this edit).

- 2026-07-05: **Vendor-side backend — full DB wiring so vendor uploads feed the buyer side.** Most vendor pages were mock/local; wired the whole side to Supabase (the buyer feed already reads `products`/`product_videos`/`vendor_profiles`, so vendor writes now surface to buyers once approved).
  - **Schema (migration `vendor_backend_phase1_schema`):** added store+KYC columns to `vendor_profiles` (`website, logo_url, banner_url, whatsapp, owner_name, owner_email, address_line, area, state, postal_code, landmark, pan, gstin, cin, profile_score, onboarding_complete`) and `products.views_count`; new tables **`catalogues`** (PDF lookbooks, `product_status` enum, public read of live), **`advertisements`** (vendor-only campaigns), **`vendor_documents`** (private KYC files) — all RLS-mirrored on the existing `product_videos`/`is_admin()` patterns; storage buckets **`product-videos`** + **`catalogues`** (public) + **`business-docs`** (private, owner/admin); admin-only RPC **`approve_vendor_content(target)`** to flip a vendor's `under_review` products/videos/catalogues to `live`. Regenerated `database.types.ts`.
  - **Publish chain (Phase 2):** `Upload.tsx` now resolves + writes `category_id` (new `resolveCategoryId` keyword→DB-category map in `queries/products.ts`, so buyer category names/filters match) and gained **Edit mode** (`/upload?id=` via `useProductForEdit`/`updateProduct`; Products list Edit links to it). **Video Closeups** (`UploadVideo.tsx`) rewired off its mock to real product tagging + `createProductVideo` (uploads video+thumb to `product-videos`, inserts `product_videos` `under_review`) / `useMyVideos` / `deleteProductVideo` — vendor videos now reach the buyer reel. **Catalogues** (`UploadCatalogue.tsx`) wired to new `queries/catalogues.ts` (upload PDF → `catalogues`).
  - **Store profile + onboarding (Phase 3):** new `queries/vendorStore.ts` (`useMyVendorProfile`/`saveVendorProfile`); `BusinessProfile.tsx` hero (brand, city, business type, PAN, MD) now reads the real `vendor_profiles`. New `queries/vendorOnboarding.ts` — the 8-step `Onboarding.tsx` now **persists** on completion: identity/address/owner/PAN·GSTIN·CIN → `vendor_profiles` (+`onboarding_complete`, `profile_score`), KYC doc rows → `vendor_documents`, and the step-7 product → a real `under_review` listing.
  - **Vendor ops (Phase 4):** new `queries/vendorDashboard.ts` — `SellerHome` analysis cards + profile score now show **real counts** (live products, open leads, inquiries, quotes sent/accepted). `Quotes.tsx` gained the live `OpenRfqLeads` panel (quote real buyer RFQs via the existing `rfqs.ts` loop). `Advertisements.tsx` "Create Your Ad" now inserts a real `advertisements` row + a "Your Campaigns" list with pause/resume/delete (`queries/ads.ts`).
  - Verified: `tsc --noEmit` + `npm run build` clean; RLS enabled with policies on all 3 new tables; demo-vendor's 6 products already `live`. Moderation preserved — vendor uploads enter `under_review` and appear to buyers only after `approve_vendor_content`.

- 2026-07-05: **Call Now — desktop shows the number on screen; full button audit.** Follow-up to the click-to-call feature.
  - **Desktop vs mobile:** new `placeCall(name, phone)` in `queries/calls.ts` — on mobile opens `tel:` (dialer), on desktop (where `tel:` no-ops) opens a **global "Call {vendor}" dialog** showing the number big, as a `tel:` link, with a **Copy number** button. New `src/lib/callStore.ts` (`useSyncExternalStore`) + `src/components/buyer/CallNumberModal.tsx` mounted in `App.tsx`. `useCallVendor` now routes through `placeCall` (still logs the call + falls back to chat when no number).
  - **Every call button audited & wired.** Product/vendor Call Now (already done) dial+log real vendors. Also wired: **ChatThread** header call button (was a toast → now dials the vendor), the two **NewArrivals** decorative "Call Now" tiles (Looking-for-these / Brand Picks, were dead), and the **Freelancers / FreelancerProfile / ServiceVendors / ServiceVendorProfile** Call buttons (were chat-only). Non-DB entities (freelancers/service vendors, decorative tiles) use a deterministic `demoPhone(seed)` so the button places a real call (show/dial) without a stored number. MyQuotes' quote Call already routed via `callVendor`.
  - Verified: `tsc`/build clean; deployed. (Note: `tel:` still can't auto-place a call — desktop just displays the number.)

- 2026-07-04: **Call Now → native dialer + logged in the Calls tab.** Every buyer "Call Now" used to just `navigate('/chats/${vendorId}')`; now it opens the phone dialer pre-filled with the vendor's number and records the call.
  - **Schema (`calls_and_vendor_phone`):** added `vendor_profiles.phone` (public-read; seeded `+91…` demo numbers for all 6 vendors) and a `calls` table (`buyer_id`, `vendor_id`, `direction` check outgoing/incoming/missed, `product_context`, `created_at`) with RLS — SELECT `buyer_id/vendor_id = auth.uid() or is_admin()`, INSERT/write `buyer_id = auth.uid()`, indexed on buyer. Regenerated `database.types.ts`.
  - **Hook `src/lib/queries/calls.ts`:** `useCallVendor()` — on click looks up the vendor's `phone`+`brand_name`, logs an outgoing `calls` row (signed-in only; invalidates `["calls",uid]`), then `window.location.href = tel:<digits>`. On desktop (where `tel:` may no-op) it also toasts the number. **No number on file → falls back to opening the chat thread** + toast. `useCalls()` returns the buyer's real calls grouped Today/Yesterday/<date> in the existing `CallRecord` shape.
  - **Wired ~13 buyer Call Now buttons** to `useCallVendor` (shared `ListingProductCard`/`BuyerProductCard`/`VideoCloseUpsViewer`; pages NewArrivals/Trends/Sale/ProductDetail/VendorProfile/SearchResults/RecentlyViewed/SavedCollectionDetail/BrowseProducts/MyQuotes) — leaving their **Chat** buttons opening chat. `Chat.tsx` Calls tab now reads real grouped calls when signed in (static fallback signed out) and the green call button dials.
  - **Limitation (by OS design):** `tel:` only *pre-fills* the dialer — a web app cannot auto-place a call. Real auto-dialing needs a telephony provider (Twilio/Exotel) — a separate integration.
  - Verified: RLS round-trip with a demo-buyer JWT (self-insert 201, spoof 403, anon read `[]`, public phone read OK); `tsc`/build clean; deployed.

- 2026-07-04: **Search Results filters — made item-focused.** The panel showed facets that weren't relevant to the searched item; rewrote `src/lib/searchFilters.ts` so filters are tight to the query.
  - **Removed the parent-category dump:** searching "t-shirt" no longer prepends a "Category" facet listing every menswear sub (Jeans, Jackets, Kurtas…). Instead each domain shows a focused **item Type** facet.
  - **Sub-item detection for tops & bottoms:** apparel-top resolves tee vs polo vs shirt vs women's-top; apparel-bottom resolves jeans vs shorts vs joggers vs trousers — each with a tailored Type + only the attributes that matter (e.g. jeans → Jeans Style / Wash / Waist / Length; shirt → Shirt Type / Collar / Sleeve). Fixed the "t-shirt contains the word shirt" mis-match by checking the tee set before the shirt set with an explicit fallback.
  - **Gender inferred, not shown:** when the query implies a gender ("mens…", "womens…", "kids…") it's applied as a pre-scope but the redundant Gender facet is hidden (new `Facet.hidden`; `SearchResults` skips hidden facets in the sheet + active-count, and Clear resets to the inferred pre-scope). Gender still shows when the query is gender-neutral ("formal shirt").
  - Verified the resolver across 13 queries (tees/jeans/polo/shirt/shorts/track pants/fabric/lipstick/shoes/bags/saree/jacket/dress) — each yields a focused, relevant facet set; `tsc`/build clean; deployed.

- 2026-07-04: **Search page — functional voice + image search.** The mic and "Photo Search" buttons were dead; both now work.
  - **Voice search (no plugin/key):** new `src/hooks/useSpeechSearch.ts` wraps the browser's built-in Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`). Tapping the mic starts listening (pulsing mic + "Listening… speak now" hint), live-fills the input with interim transcripts, and runs the search on the final result. Graceful `supported` flag → toast on Firefox/unsupported. Works in Chrome/Edge/Safari over HTTPS after the mic prompt.
  - **Image search:** "Click a photo" (camera `capture="environment"`) and "Select a photo" (gallery) trigger hidden file inputs; the chosen image shows in an "Analyzing image…" overlay while it's sent to a new **`image-search` Supabase Edge Function** (`supabase/functions/image-search/index.ts`, `supabase.functions.invoke`). The function calls a vision model (**OpenAI `gpt-4o-mini`** default, `IMAGE_SEARCH_MODEL` overridable; image sent at `detail:"low"` for speed/cost) to turn the photo into a 3–6-word product query, which the page then runs against the normal catalogue search. CORS-enabled, `verify_jwt` on (anon/user JWT passes). Returns `{error:"not_configured"}` until the key is set, and the client shows a friendly provider-neutral toast for that + other failures.
  - **Requires (image only):** an `OPENAI_API_KEY` secret in Supabase → Edge Functions → Secrets (voice needs nothing). Verified: function deployed + reachable (`not_configured` without key), `tsc`/build clean, deployed to production. (Originally scaffolded on Anthropic Claude, switched to OpenAI at the user's request.)

- 2026-07-04: **Completed the Saved (wishlist) + Recently-Viewed migration to Supabase.** Both `useSyncExternalStore` stores are now **DB-aware without any consumer changes** — the synchronous public API (`useSaved`/`openSaveModal`/`saveToFolders`/`createFolder`/`renameFolder`/`deleteFolder`/`removeFromFolder`/`unsaveProduct`; `useRecentlyViewed`/`recordView`/`removeRecent`/`clearRecent`) is preserved.
  - **Bridge:** new `src/components/StoreSync.tsx` (mounted in `App.tsx`) pushes `useAuth().user?.id` into `setSavedUser()` / `setRecentUser()`. On sign-in the stores **hydrate from the DB** (replacing the local seed) and mirror every mutation fire-and-forget; on sign-out they revert to the localStorage seed. `commit()` skips localStorage while signed in (DB is source of truth).
  - **Saved model:** `saved_items` = the "All Saves" master set; `saved_folders` + `saved_folder_items` = named folders. `createFolder` generates a client-side UUID when signed in and inserts it explicitly so local ids === DB ids (the modal's create-then-select flow keeps working). Hydration rebuilds rich `SavedProduct`s by joining `products` (new shared `fetchProductCardsByIds` in `queries/products.ts`). Recently-Viewed hydrates `recently_viewed` the same way. Only real product UUIDs are written (FK-guarded); synthetic/demo ids stay local-only.
  - **Verified:** `tsc` + build clean; RLS round-trip with a real demo-buyer JWT — own insert/read 201/200, **spoofing another buyer's `buyer_id` blocked (403)**, anon read `[]`; deployed, `/saved` + `/recently-viewed` return 200. A new signed-in user now starts with a real (empty) wishlist/history instead of the seeded demo items.

- 2026-07-04: **Buyer-side production audit — feeds moved off hardcoded arrays onto the live Supabase catalogue.** Audited every buyer surface for hardcoded vs DB data and wired the content feeds to real data, keeping graceful local fallbacks so nothing renders empty while loading / signed out.
  - **New schema (migration `buyer_side_tables_phase6`):** `product_videos` (Instagram-reel closeups: category/brand_line/price/moq/rating/reviews/likes_count/views_count/thumbnail_url/video_url/status), `follows` (buyer→vendor), `recently_viewed`, `saved_folders` + `saved_items` + `saved_folder_items`, plus a `products.compare_at_price` column for the Sale strikethrough/discount. RLS on all (public read of live content, `auth.uid()`-scoped writes) — advisor shows no new gaps.
  - **Richer seed:** 5 additional vendor accounts (`seed_vendors_and_categories` — real `auth.users` + `vendor_profiles`), 5 extra categories (Kidswear/Winter Wear/Trousers/Footwear/Accessories), **20 products across genders/categories** (`seed_products_catalogue`, 16 with `compare_at_price` → Sale), **7 product videos** (`seed_product_videos`), 8 follows, and 3 real conversations (8 messages) for the demo/real buyers. Now **26 live products / 6 vendors / 11 categories**.
  - **Query layer:** `src/lib/queries/videos.ts` (`useVideoCloseUps`), `follows.ts` (`useFollowing` — brands = real vendors + follow state, DB when signed in / local store when out), `vendor.ts` (`useVendorProfile` — identity + own products + reels), plus feed selectors on `products.ts` (`sortTrending`/`filterSale`/`filterForYou`, exported `mapProductRow`/`PRODUCT_CARD_SELECT`) and `useConversations` on `chat.ts`. `ProductCardData` gained `priceValue`/`compareAtPrice`/`discountPct`/`gender`/`categoryName`/`soldCountNum`. **One cached `["products","live"]` query feeds New Arrivals, Trends, Sale, For You, Following and Search** (no duplicate requests).
  - **Pages wired to DB:** Video Closeups (`VideoCloseUpsPage` + New Arrivals strip — the reel viewer was already Instagram-style; now fed by `product_videos`), **New Arrivals** (real "we recommend" + real username), **Trends** (brand/styled/feed product grids), **Sale** (real discounted catalogue), **For You** (real catalogue mapped through the preference filter), **Following** + **FollowingViewAll** (real vendors + `follows` writes), **VendorProfile** (real name/location/followers/verified/about/products/reels/follow — replaced the static "CARAMEL"), **Chat hub** (real conversations), **Search** (real recommendation feed, still filtered by trending chips). Also fixed the cards' placeholder "Product name" to render the real product name.
  - **Deferred (tables + RLS ready):** the Saved (wishlist folders/modal) and Recently-Viewed localStorage stores still persist locally; their DB migration is isolated for a follow-up to avoid destabilising the global save-modal. Verified: `tsc` + build clean, anon REST returns feeds (products+category+images, videos) while `follows` is correctly empty for anon; deployed to production.

- 2026-07-04: **Added Gujarati (ગુજરાતી) as a third language.** Generalized the i18n store from en/hi to multi-language: `Lang` is now `"en" | "hi" | "gu"`, `LANG_OPTIONS`/`SUPPORTED`/`read()`/`setLang()` updated, and `langCodeFromName()` maps Gujarati/ગુજરાતી → `gu` (checked before Hindi). Added a full **`GU` dictionary** in `i18n.ts` mirroring every `HI` key (~180 entries), collected into `DICTS = { hi, gu }`; `translate()`/`lookup()` now take the language and read from `DICTS[lang]`. Updated `AutoTranslate.tsx` to translate for any non-English language and added a `LAST_SET` WeakMap so **direct hi↔gu switches** translate from each node's stored English rather than from the other language's already-rendered text (previously the restore/translate logic assumed only hi↔en). Added the **ગુજરાતી** button to the vendor MyStore selector; the buyer Login modal and Profile → Regional Settings already listed Gujarati and now resolve it via `langCodeFromName`. Verified in Playwright: GU `/profile` shows 30 Gujarati body strings, English fully restores (0 Devanagari/0 Gujarati), and a **live en→hi→gu→en** switch via Regional Settings flips the nav through श्रेणियाँ → શ્રેણીઓ → Categories with no reload and no errors. Build clean.

- 2026-07-04: **Language now changes EVERYWHERE — global DOM auto-translator.** The per-component `useT()` wrapping only translated strings each page explicitly opted in, so switching to Hindi changed the nav but left most page bodies in English ("not changing from everywhere"). Added `src/components/i18n/AutoTranslate.tsx` — mounted once at the app root inside `<BrowserRouter>` — which walks the rendered DOM and translates any text node / `placeholder` whose English matches the dictionary, with **no per-component wrapping**. It: re-applies on language change and on route/content changes via a `MutationObserver` (rAF-batched), stores each translated node's original English in a `WeakMap` to restore on switch-back, is idempotent (React re-renders that reset text get re-translated), skips `script/style/textarea/code/pre` and `[data-no-translate]`, and leaves dictionary-missing strings as English. Also **greatly expanded** the `HI` dictionary in `i18n.ts` (~180 entries: common actions/buttons, auth, commerce, RFQ/quotes, profile, chat/support, vendor dashboard, sort/filter, section headings) and exported `lookupHi()`. Net effect: one language switch now translates page bodies across the whole app (buyer + vendor), not just chrome. Verified in Playwright: EN pages have 0 Hindi nodes; HI `/profile` shows 26 translated body strings (गतिविधि, खाता सेटिंग्स, मेरी समीक्षाएँ, व्यवसाय विवरण…), HI `/search/results` translates Brand Picks/sponsored/Call Now/Sort/Filter; switching back to English fully restores; no console/page errors. Build clean.

- 2026-07-04: **Functional language translation (English / Hindi) — vendor + buyer.** The three previously-disconnected, non-functional language selectors now drive one shared global switch that actually translates the UI. New `src/lib/i18n.ts` — a module-level store (`useSyncExternalStore` + `localStorage` key `cosora.lang`, sets `document.documentElement.lang`) exposing `useLang()`, `setLang()`, `langCodeFromName()`, and `useT()`. `useT()` returns a translate fn keyed by the **English string** (`t("New Arrivals")`), so components wrap existing text with zero key bookkeeping and any unlisted string safely falls back to English. Scope = app **chrome** (nav, buttons, headings, labels); dynamic data (product names, vendor descriptions, chat/user text) intentionally left as entered — chosen approach per the user (global switch + curated dictionary, EN+HI to start; extensible). **Selectors wired:** buyer Login `LanguageModal`, buyer Profile → Regional Settings `<select>`, and vendor MyStore English/हिंदी buttons all call `setLang()` (Login/regional map any language name → en/hi; only those two are translated today, others fall back to English). **Chrome translated:** buyer `MobileBottomNav` + seller nav, `DashboardSidebar` (buyer & seller menus + Switch Mode/Support labels), `BuyerHomeTabs`, the inline home-feed tab strip on NewArrivals/Trends/Sale/ForYou/Following, `BuyerTopBar` (drawer menu, Buyer/Seller switch, search placeholder, currency/support), `SubmitRequirementCard`, and SearchResults (FILTER/SORT, Call Now, filter-sheet Filters/Refining/Clear/Show). Verified in Playwright: buyer mobile nav + feed tabs render Hindi with `docLang=hi`, English default unaffected, vendor sidebar renders Hindi, and a **live switch** via Regional Settings updates the nav instantly + persists to `localStorage`. Build clean.

- 2026-07-04: **Context-aware Search Results filters.** The filter panel now adapts to *what* the buyer searched — different searches surface different, relevant facets. New `src/lib/searchFilters.ts` defines ~16 product domains mirroring the category taxonomy (what Cosora sells) with a `resolveFilterSchema(query)` resolver: e.g. **"mens shorts"** → *Men's Shorts* (Category-within-Menswear, Gender, Fit, Fabric, Waist Size, Length, Pattern, Colour, GSM, Price, MOQ, Rating, Vendor) with **gender pre-scoped to Men**; **"fabric"** → Fabric Type / GSM / Width / Construction / Composition; **"lipstick"** → Product Type / Skin Type / Formulation / Concern / Finish; plus footwear (Sole, UK Size), bags, packaging (Material, Printing), trims, home textile, ethnic (Work/Embellishment, Occasion), dresses, winterwear, innerwear. Each schema prepends a **Category facet built from `CATEGORY_TAXONOMY`** (the searched category's sibling subcategories). Domain matching is whole-word + plural-tolerant so "bag" doesn't fire inside "poly**bag**" (Packaging ordered before Bags for "paper bag"). Rebuilt `SearchResults.tsx`: replaced the static GENDER/SORT/FILTER dropdowns with **SORT + a context-aware FILTER bottom-sheet** ("Refining {domain}", chip facets, live "Show N results", Clear-all, active-count badge on the bar). Filtering engine (`matchFacet`) maps facets to real product fields (gender/fabric/fit/price-bucket/gsm-bucket/rating/verified) and derives a stable pseudo-value (`hashPick`) for facets without backing data, so every chip narrows deterministically. framer-motion sheet, safe-area footer, `useReducedMotion`-gated. Verified in Playwright across 8 queries (correct domain + facets each, gender pre-scope, live count changes, **no dup facets, no console errors**); build clean.

- 2026-07-04: **Full route audit + built the last 2 missing pages; removed 3D configurator.** (1) Ran a static route-audit (104 distinct nav targets — `navigate(...)`/`to=`/`href=` incl. template literals — cross-checked against 80 real routes + scaffold routes): **0 broken routes** (nothing falls through to NotFound). The only gaps were the 2 remaining `BuyerRouteShell` "Phase 1" scaffolds still linked from Profile — now built as real pages. (2) **My Reviews** (`src/pages/MyReviews.tsx`, `/profile/reviews`) — buyer's written reviews: summary (count + avg-given stars), All/Vendors/Products/Services filter chips (coral active + counts), review cards (thumb, type badge, star rating, date, body) with a kebab menu → **Edit** (bottom-sheet with interactive star picker + textarea, persists to local state) / **Delete** (animated removal + toast), and an empty state. framer-motion stagger, `useReducedMotion`-gated, MobileBottomNav. (3) **Support Chat** (`src/pages/SupportChat.tsx`, `/profile/help/chat`) — full-page "Chat with Us" support thread reached from Profile → Help & Support: coral agent header (avatar + online + call), the **legally-required chat-monitoring disclosure** banner (`CHAT_MONITORING_NOTICE`), support/user bubbles with a canned auto-reply + typing indicator, attachment menu (Camera/Gallery/Audio/PDF via hidden inputs), `100dvh` layout with safe-area-inset input bar. Coral accent for buyer-side color consistency. Wired both real routes, removed their scaffold entries from `buyerShellRoutes`. (4) **Removed the 3D garment configurator entirely** — deleted `pages/Configurator.tsx` + `lib/configuratorData.ts`, and every reference (App.tsx import/route + `/product/:id/3d-configurator` scaffold, BuyerTopBar "Design Studio" link, PostRequirement "Design a Custom Garment" card). Re-ran the audit: **0 broken, 0 scaffold-only linked**. Verified both new pages in Playwright at 390px (render, no `Route scaffold`, **0 overflow, no console errors**); build clean.

- 2026-07-03: **Mobile "whole site zoomed out into one frame" fix + smaller buyer logo.** (1) `BuyerTopBar` header (`logo → search(flex-1, max-w-md) → bookmark → 3-dot`) overflowed the viewport by **49px** on phones: the search button had no `min-w-0`, so its flex `min-width:auto` equalled the full no-wrap placeholder width and refused to shrink, pushing the icons past the right edge → mobile browsers scaled the whole page down to fit. Added `min-w-0` to the search button (text already truncates). Surfaced after the wordmark became an image (wider than the old text). Verified 0px horizontal overflow at 390px across landing + all buyer pages (home/for-you/categories/profile/saved/chats/search/browse). (2) Shrank the buyer-side `CosoraLogo` a notch — top bar `24→20`, side drawer `22→19` (proportional, bbox-crop preserves shape).

- 2026-07-03: **OTP page — Previous/Next stay above the number pad.** On `OtpVerify.tsx` the Previous/Next bar sat in normal flow at the bottom of a `min-h-screen` container, so the Android number pad covered it. Added a `useKeyboardInset()` hook (VisualViewport API: `innerHeight − visualViewport.height − offsetTop`) and made the action bar `fixed bottom-0`, translating it up by the keyboard height so it stays visible/tappable while entering the OTP (returns to the bottom when the keyboard closes). Page-scoped (no global viewport-meta change), safe-area-inset aware, smooth transition. Verified with a mocked VisualViewport in Playwright: bar is `position:fixed`, lifts `translateY(-320px)` for a 320px keyboard, and resets to `none` on close — no console errors; build clean.

- 2026-07-03: **Brand logo → image wordmark + preference-page images.** (1) Replaced the CSS text wordmark with the provided PNG (`src/assets/Buyer/logos/Cosoralogo.png`) across the whole buyer side, landing page and registration flow. New shared `src/components/CosoraLogo.tsx` — the source is a 500×500 canvas with the wordmark centred in transparent padding, so the component **CSS-crops to the exact measured wordmark bounds** (decoded once: L37/T199, 420×86) and renders tight at any `height`; a `variant="white"` inverts the red source to white for dark backgrounds. Swapped in: `BuyerTopBar` (top bar + drawer), `Landing` (nav responsive + footer; dropped the old `landing page/Cosoralogo.png` import), `Search`, `SearchResults`, `FollowingViewAll`, `PostRequirement` (white, on coral), and the registration screens `Login` / `OtpVerify` / `RoleSelection` / `AccountInfo` (red) and `Welcome` / `Terms` / `Register` (white/red per background). Product-image **watermarks** (tiled "COSORA" over photos) were intentionally left as text — those aren't the brand mark. (2) `InterestPreference.tsx` (buyer registration preference step) now uses the 13 provided thumbnails from `src/assets/Buyer/Preference/` via `import.meta.glob` (handles the spaces / `&` / curly-apostrophe filenames), keeping an Unsplash fallback only for the 4 interests without a supplied image (Cosmetics, Fashion Designer, Marketing PR, Photography). tsc clean; build clean; verified in-browser (buyer bar, landing, white-on-dark Welcome, Login, and all preference tiles load with **no broken images / no console errors**). Pre-existing `OtpVerify:19 as any` lint error is unrelated and untouched.

- 2026-07-03: **Buyer Categories page (new).** Built the real category-browse destination the bottom-nav "Categories" tab was missing (it had been pointing at `/search/results` as a documented stopgap). New taxonomy in `src/lib/categoryTaxonomy.ts` — condensed from `COSORA_CATEGORY_TAXONOMY.md` into 11 top-level categories (Menswear, Womenswear, Kids' Wear, Footwear, Fashion Accessories, Beauty & Cosmetics, Packaging, Raw Materials, Trims & Home Textile, B2B Services, Freelancer Services) with ~150 subcategories, optional group headers (e.g. Womenswear → Lingerie & Shapewear; Trims → Home Textile), per-category icon + banner image + image pool, and a `subCategoryHref` router. New `src/pages/Categories.tsx` at `/categories` (BuyerShell): "Shop by Category" header, **cross-category search** that flat-indexes every subcategory (icon + breadcrumb result rows), a sticky **left category rail** (icon tiles, animated `layoutId` active marker) and a right panel with an **animated banner + grouped subcategory image grid** (crossfade on category switch, stagger-in tiles, tap scale). Product subcategories route to `/search/results?category=<label>`, B2B Services → `/services`, Freelancer subcategories → `/freelancers`. Wired the route and repointed MobileBottomNav "Categories" → `/categories` (removed the stopgap comment). Tile images marked decorative (`alt=""`) so screen readers announce each label once. framer-motion throughout, `useReducedMotion`-gated. tsc + eslint clean; 10 Playwright assertions pass (rail switch, grouped headers, search, product/service routing) with no console errors; build clean.

- 2026-07-03: **Buyer Chat redesign — Messages hub + Calls + conversation thread.** Rebuilt `src/pages/Chat.tsx` off the vendor `DashboardLayout` desktop split-pane into a mobile-first buyer experience, and added a real conversation page. New shared data `src/lib/chatData.ts` (`Conversation`/`ChatMessage`/`CallRecord`, `CONVERSATIONS`, `CALLS`, `callGroupsInOrder`, `getOrCreateConversation`, `CHAT_MONITORING_NOTICE`).
  - **Messages hub** (`Chat.tsx`, `/chats` + `/chat`): "Messages / N Conversations" header + "N online" badge, animated **Chats / Calls** segmented tabs (`layoutId`), search, chat rows (avatar + online dot, name, time, preview, **coral Quote chip → `/requirement/my-quotes`**, unread badge), and a blue **Post Requirement** FAB. **Calls tab** grouped by date headers (Today / Yesterday / Jan 20) with direction icons (green Outgoing/Incoming, red Missed) and **time-only** rows per the reference note. Tab is URL-driven (`/chats?tab=calls`) so Profile's "Calls" stat deep-links to it (was the dead `/chats/calls` scaffold).
  - **Conversation thread** (new `src/pages/ChatThread.tsx`, `/chats/:vendorId`): header with avatar+online, **name/rating/location → vendor profile**, green call, and a 3-dot menu (View Quote details, View Vendor Profile, Mark as Read, Star, Archive, **Report**, Block Vendor, Delete). **RFQ banner → quotes page**, legally-required **chat-monitoring disclosure**, Today divider, coral (sent) / white (received) bubbles with timestamps + read ticks. Attachment grid (Gallery / Camera / Document / Contact / **Audio "records"**) via hidden file inputs; input bar with **mic ↔ send toggle** (animated). **Report modal** → reasons list → "Thanks for your feedback" → Block/Close. `getOrCreateConversation` synthesizes a usable thread for any vendor id, so every Chat/Call button across the app (25+ call sites) now opens a real thread instead of the old `BuyerRouteShell` scaffold.
  - **Routing**: imported `ChatThread`, added `<Route path="/chats/:vendorId">`, removed the `/chats/:vendorId` and `/chats/calls` scaffolds from `buyerShellRoutes`. Verified with a **route-audit script** (270 nav targets vs 80 real routes / 12 scaffolds): 0 NotFound fall-throughs; scaffold-only targets dropped from 5 → 2 (remaining: `/profile/reviews`, `/profile/help/chat` — pre-existing, out of scope). Fixed a button-in-button DOM-nesting warning in the hub rows. tsc + eslint clean; 24 Playwright assertions pass with no console errors; build clean.

- 2026-07-03: **Unified "Submit Requirement" card.** The "Looking for products? → Submit Requirement" block existed in three inconsistent designs (the polished coral-corner card on Search Results' Brand/empty state vs. plainer variants in the New Arrivals feed and For You). Extracted a single shared `src/components/buyer/SubmitRequirementCard.tsx` using the Search-Results design everywhere: rounded card with **coral corner accents**, centred "Looking for products?" heading, full-width coral **Submit Requirement** CTA (whileTap, reduced-motion-gated), and a "Get quotes from verified manufacturers" caption. Optional `onQuickRfq` prop adds the feed quick-action rows (**Quick RFQ · Create New Requirement · My Previous Quotes**), so the New Arrivals feed keeps that functionality inside the new design. Replaced the local `SubmitRequirementBox` (NewArrivals), the minimal bottom bar (ForYou), and `EmptyRequirement` (SearchResults, ×2 call sites) with the shared component; pruned now-unused icon imports. tsc + eslint clean; verified in-browser (Search Results Brand tab + New Arrivals feed card with rows), no console errors.

- 2026-07-03: **Service Vendors — listing redesign + new profile page.** Shared data in `src/lib/serviceVendorsData.ts` (8 vendors across the 7 service categories with full profile fields: serviceType badge, productCode tag, cover carousel, tags, price, about, services offered, experience/turnaround/priceRange, portfolio, reviews; `SERVICE_CATEGORIES`, `getServiceVendor(id)`). Rewrote `src/pages/ServiceVendors.tsx` to **BuyerShell + coral** (was vendor `DashboardLayout`): "Service Vendors" header + subtitle, search, **Filters(sort) / Location ⌄ / Rating 4+** control row (functional dropdowns), category pills with icons (All Services / Printing & Manufacturing / Logistics & Supply Chain / IT/Software & SaaS / Finance & Compliance / Marketing/PR/Photography / Machinery & Equipment), "N vendors found" + **grid/list view toggle**. **Grid** cards: image with coral **service-type badge**, name, rating + city, tags, **larger price**, Chat/Call. **List** cards: thumbnail left + name, service-type badge, rating(count) + full location, tags, price, Chat/Call. New `src/pages/ServiceVendorProfile.tsx` at `/services/:vendorId` (was a scaffold; removed the 3 `/services/:vendorId*` scaffolds, added a real `<Route>` before the shell map): back header + bookmark/share, **cover carousel**, identity (name, coral service-type badge, rating+reviews, location, **product-code tag**), animated **Services / Portfolio / Reviews** tabs (`layoutId="sv-tab"`) — Services (About, Services Offered w/ prices, **Service Details** card: Experience / Turnaround Time / Price Range), Portfolio grid (title+category overlay), Reviews (score + list + **Write a Review** modal), Chat/Call Now + "Usually responds within 24 hours". Mirrors the FreelancerProfile pattern. framer-motion throughout; `MobileBottomNav`. Verified end-to-end (22 Playwright assertions, no console errors); build clean.

- 2026-07-03: **Buyer Help page — removed the vendor top bar.** `src/pages/Help.tsx` (routes `/profile/help`, `/help`) was wrapped in the vendor `DashboardLayout`, which rendered the New Arrivals/Trends/Services/Freelancers tab strip inside the buyer profile section. Replaced it with the standard profile sub-page pattern: a sticky **back-header** ("Help & Support" → `/profile`) + content + `MobileBottomNav` (no tab strip). Removed the `-m-4 lg:-m-6` negative margins that had counteracted DashboardLayout's padding.

- 2026-07-03: **Home tab bar centering** — The `NEW ARRIVALS / TRENDS / SALE / FOR YOU / FOLLOWINGS` strip was left-aligned on **Trends**, **For You**, and **Following** (New Arrivals + Sale already centered). Added `justify-start lg:justify-center` so the tabs center on desktop while staying left-aligned + horizontally scrollable on mobile (no tab clipped off the left edge).

- 2026-07-02: **Buyer Search Results page + GENDER/SORT/FILTER components** — New `src/pages/SearchResults.tsx` at `/search/results` (was a `buyerShellRoutes` scaffold; removed `/search/results` + `/search/filters` scaffolds, added real `<Route>`). Reads `?q=` / `?category=` via `useSearchParams`. Header: COSORA + back + query bar (tap/clear → `/search`) + bookmark. **Product 9,999+ / Brand N tabs** (animated `layoutId` underline). Filter bar: **GENDER** (Men/Boys/Girls/Women), **SORT** (What's new / Price high-low / Popularity / Discount / Price low-high / Customer Rating), **FILTER** (Verified/TrustedSEAL only) — each opens an animated dropdown panel with a scrim; all functional (gender filters, sort reorders). 2/3-col grid toggle. Product cards show **TrustedSEAL image** for verified vendors, bookmark → `openSaveModal`, Call Now. **Brand Picks** sponsored strip at top; infinite scroll; **"Looking for products? → Submit Requirement"** empty state (red corner accents) for no results / empty Brand tab. Now the real destination for every surface that links `/search/results`: BuyerTopBar drawer "Browse Products", MobileBottomNav **Categories**, home category tiles, Search runs, Trends "View More". framer-motion throughout, `useReducedMotion`-gated.

- 2026-07-02: **Freelancers & Experts — listing redesign + new profile page.** Shared data in `src/lib/freelancersData.ts` (8 freelancers across the 7 categories with full profile fields: cover carousel, hourly/project rate, experience/turnaround/price-range, tags, about, services offered, skills, education, portfolio, reviews; `getFreelancer(id)`). Rewrote `src/pages/Freelancers.tsx` to **BuyerShell + coral** (was vendor `DashboardLayout`): "Freelancers & Experts" header, search, **Location / Rating / Filters(experience) / Sort** controls (functional), category pills (All Experts / Fashion Designing / Tech Pack Creation / Fabric & Trim Sourcing / Performance & Influencer Marketing / Photography & Videography / UI/UX & Website Development), "N freelancers found", 2-col cards (avatar + online dot, name, role, rating+reviews, City·Country, tags, **large ₹/hr price**, Chat/Call) → link to profile. New `src/pages/FreelancerProfile.tsx` at `/freelancers/:id` (was a scaffold; removed the 3 `/freelancers/:id*` scaffolds + a duplicate `/freelancers` route): back header + bookmark/share, **cover carousel**, identity (avatar/name/role/specialty, rating+reviews, location), **Hourly Rate + Per Project** cards, animated **Services / Portfolio / Reviews** tabs — Services (About, Services Offered w/ prices, Skills & Expertise stat cards + tags, Education), Portfolio grid (title+category overlay), Reviews (score + list + **Write a Review** modal), Chat/Call + "Usually responds within 24 hours". framer-motion throughout; `MobileBottomNav`.

- 2026-07-02: **Garment Configurator (Design Studio)** at `/configurator` — buyer custom-apparel designer inspired by virtualthreads.
  - **v2 (current) — realistic photo-mockup approach.** The initial three.js version looked game-y (procedural meshes) and virtualthreads is actually 2D photographic compositing, so it was rebuilt: **removed three.js/@react-three deps + deleted `GarmentScene.tsx`**; reverted the App lazy-load. Now `src/lib/configuratorData.ts` carries a `MOCKUPS` map (real Unsplash garment photos front/back per garment + a `print` zone as % of the image). `src/pages/Configurator.tsx` renders `GarmentMockup`: real garment photo (`object-cover`, with an `onError` fallback), **recolor via a `mix-blend-mode: color` tint** (preserves real fabric shading; white = natural photo, skipped tint), and the **design composited into the print zone with `mix-blend-mode: multiply`** so it reads as ink on fabric (light text auto-switches to `normal` blend so it stays visible). The garment itself is the editable surface — drag text/artwork directly on it. Tools: garment picker (10 types across tops/fleece/bottoms), Add Text (font/color/scale/rotation), Upload Artwork (PNG/JPG/SVG), color swatches, fabric+GSM, print method, sizes+quantity. Finish → **Save & Request Quote** (localStorage + route to `/requirement/post-requirement`). Entry points: BuyerTopBar drawer "Design Studio" + "Design a Custom Garment (3D)" card in the Requirement hub. Bundle back to ~2.2 MB (no WebGL, works on all devices). Tradeoff: the recolor tints the whole frame (no per-image garment mask) and print-zone rects are generic per archetype — swap in transparent-PNG mockups + per-image zones for pixel-perfect placement later.
  - **Polish pass:** garment-image **crossfade** on change (AnimatePresence), **smooth recolor** transition (softened to 0.92), studio radial backdrop + vignette, rounded-3xl preview, loading pulse, section **stagger-in** + micro-interactions (whileTap/hover), animated **layoutId front/back toggle**, **grouped garment picker** (Tops/Fleece/Bottoms), crisp non-blended **selection outline + in-canvas delete (X) button**, animated selected-layer panel, empty-zone hint. All `useReducedMotion`-gated.

- 2026-07-02: **Buyer Product Detail redesign** — Rewrote `src/pages/ProductDetail.tsx` to match the reference and buyer design system. Now uses **BuyerShell** (was the vendor `DashboardLayout`) + coral `#ef4d62` (was theme tokens). New **center-peek swipeable media carousel** (`MediaCarousel`): neighbours peek, drag/dots, **tiled COSORA watermark**, **video autoplay** when a video slide is active (`<video autoplay muted loop>`, sample mp4 added to media), rating badge, **TrustedSEAL image only when `isInAd`**. Sections: breadcrumb, name + bookmark(→`openSaveModal`) + share(copies link), ₹price/unit, MOQ + product code, **clickable vendor card** (→`/vendor/:id`, Follow toggle, Trust Seal Verified when ad). Details/Reviews **animated tabs** (`layoutId`). Details: colors, sizes, **inline Chat (filled) + Call Now (outline)**, **collapsible Product Specifications** (+ Add Fabric, Download PDF), Customization YES/NO, Description (+Translate), Manufacturing (Made in + certs + View Profile). Reviews: 4.x score + animated breakdown bars + review list (Helpful vote, size bought, photos) + **Write a Review modal**. **Brand Picks** sponsored strip + **You might also like** using the shared `ListingProductCard`. Records view via `recordView`. Note (not built): a Service/Freelancer product variant should mirror the vendor upload fields — deferred.

- 2026-07-02: **Search suggestions use the standard listing card** — Swapped the "we recommend" feed on `Search.tsx` from `BuyerProductCard` to the shared `ListingProductCard` so it matches the rest of the buyer side exactly (bookmark/wishlist icon, rating badge, `₹499 | MOQ: 2 | 800+ sold`, name + bold manufacturer, location, Fabric/GSM, Fit Type, COSORA watermark, Call Now). Recommendation data reshaped to `ListingProduct` (+ `trendCat` for the trending-chip filter). Also earlier: `BuyerProductCard` heart→bookmark so any remaining usages show the wishlist icon.

- 2026-07-02: **Buyer Search — interactivity refinements** — (1) The "we recommend" name is now the **real user's first name** from `profileStore` (`useProfileState().profile.fullName`), not hardcoded. (2) **Trending Keywords are now selectable filters**: each chip maps to a `TrendCat`; tapping one **darkens it** (active = solid `bg-gray-900`, hides its thumbnail) and **filters the recommendation feed** below to that category (first chip active by default; tap again to clear → all). Recommendation pool re-tagged with categories (~3 per trend); feed resets to batch 1 on filter change. (3) **Popular keywords slider is now swipeable** (framer-motion `drag="x"` + `onDragEnd` page change, grab cursor) in addition to the dot indicators.

- 2026-07-02: **Buyer Search page — aligned to detailed references** — Reworked `src/pages/Search.tsx` (route `/search`, already reachable from `BuyerTopBar`'s search bar on every buyer page). Default state: photo search (Click/Select a photo), Recent keywords (deletable + Delete all, animated add/remove), **Popular keywords now paginated** (6 per page, numbered 1–12, animated page swap + dot indicators; dated stamp; meant to refresh daily from a Google-Trends-style job), Trending Keywords (image chips), **"Anandita Mitra, we recommend · Sponsored"** recommendations with **infinite scroll** (IntersectionObserver, paused while searching), Father's Day gifting strip, Popular search categories. Typing state: animated scrim + dropdown with **top-5 product/category suggestions (result counts + match highlight)** then **5 brand stores (logo + verified + followers)** → brand taps route to `/vendor/:id`. Added framer-motion throughout (page/section stagger, dropdown fade/slide, keyword-page swap, tap feedback) gated by `useReducedMotion`. Coral mic accent.

- 2026-07-01: **Buyer Sale page** — New `src/pages/Sale.tsx` at `/home/sale` (the SALE home tab existed in `HOME_TABS` but had no route/page; added `<Route>` + import). Follows the buyer design language exactly (BuyerShell + home tabs, coral `#ef4d62`, framer-motion page/section/list variants, shared card format). Sections: coral **MEGA SALE hero** with live session-scoped **countdown** (`Countdown` component + `useTick`, D/H/M/S) and Quick RFQ button; **Flash Deals** (deepest-discount curated row, pulsing bolt + H/M/S countdown, horizontal snap scroll); **Shop by Discount** tier chips (All / Up to 25% / 25–50% / 50%+, functional filter); **All Deals** grid with sort (Biggest Discount / price asc-desc), 2/3-col toggle, infinite scroll, empty state, and a mid-feed "Post RFQ" CTA. `SaleCard` shows a `% OFF` badge, strikethrough original vs sale price, **TrustedSEAL image** for verified vendors, bookmark → `openSaveModal`, and Call Now. Uses `QuickRfqModal`.

- 2026-07-01: **Buyer-facing Vendor Profile** — New `src/pages/VendorProfile.tsx` at `/vendor/:id` (was a `buyerShellRoutes` scaffold; removed the `/vendor/:id`, `.../products`, `.../reviews` scaffolds and added a real `<Route>`). Mirrors the vendor-side `BusinessProfile.tsx` but view-only + buyer coral theme: hero (banner, watermark, **TrustedSEAL image** from `@/assets/Trustedseal.png`, back button, followers/all-items, "Manufacturer | Exporter"), **Follow/Unfollow + Chat + Call Now** (chat added per note), About Us, Contact Details, Reviews & Ratings (coral animated bars + **Write a Review** modal + View all reviews), collapsible Detailed information (Business Type…PAN, read-only **Capacity** tiers shown only for manufacturers, **Sells** chips, Website), Brand's Categories (h-scroll), Brand's Recommendations (top enquired 6), Brand Product Videos (+ video modal), and All Products (search + gender/sort/filter + 2/3-col toggle, Call Now cards). Scroll-aware sticky header animates in past the hero. Uses `MobileBottomNav` directly (no BuyerTopBar) + framer-motion page/section/list variants. All product bookmarks route through `openSaveModal`. Routed from every buyer surface that links `/vendor/:id` (product cards, ProductDetail vendor card, Trends, Following, Search) and from the Quotes **View Profile** button (`QuoteDetailsModal` now navigates to `/vendor/:vendorId`). Follow state is local (not yet persisted to `followingStore`); banner/product imagery uses existing placeholder assets.

- 2026-07-01: **Buyer "My Previous Quotes" page** — Built the buyer quotes-management flow at `/requirement/my-quotes` (previously mis-pointed at the vendor `<Quotes />` page — now `<MyQuotes />`). New data/store: `src/lib/quotesData.ts` (RFQs + received `VendorQuote`s, `fmtMoney`, per-vendor `currency` + INR-normalized `priceINR`) and `src/lib/quotesStore.ts` (`useSyncExternalStore` + localStorage; `setQuoteStatus`, `quotesForRfq`, `rfqStats`, `bestPrice`/`topRated` ranked on `priceINR`). Page `src/pages/MyQuotes.tsx` has two internal views: **My Quotes** (stats: Active RFQs / Total / New; request cards with status + "N new" badge, image on right) and **Quotes Received** (2×2 stats, Best Price + Top Rated cards, search + status filter + sort, per-quote cards). Components under `src/components/buyer/quotes/`: `ReceivedQuoteCard` (status-driven Reject/Shortlist/Accept actions), `QuoteDetailsModal` (vendor profile, attachments images/videos/docs, vendor-quote grid incl. **Fabric** + Sample Timeline, Payment Terms input, Vendor Notes, chat-with-vendor), `VendorChatModal` (pop-up chat, buyer coral bubbles), `CompareTable` (up to 3 quotes; rows Price/MOQ/Lead/Fabric/Rating/Actions). Accept→green banner, Reject→red banner. Removed 3 superseded scaffold routes (`.../quotes-received`, `.../quote-detail/:quoteId`, `.../compare`) from `buyerShellRoutes` in `App.tsx`. Currency note honored: Indian vendors show ₹, foreign vendors (e.g. Bangladesh) show their currency; ranking uses `priceINR` so cross-currency Best Price is correct (₹245 Textile Masters).

- 2026-07-01: **Buyer-side build-out (major)** — Built/rewrote the buyer experience. New reactive state pattern used throughout: module-level stores backed by `useSyncExternalStore` + `localStorage` (no React context), so bookmarks/follows/prefs/views stay in sync across every page. Buyer pages use `BuyerShell` (`src/components/buyer/BuyerShell.tsx` — BuyerTopBar + content + MobileBottomNav + ToTop), NOT the vendor `DashboardLayout`.
  - **Product Detail (buyer)** — `src/pages/ProductDetail.tsx` rewritten: media carousel w/ COSORA watermark, vendor card, Details/Reviews tabs, specs grid, sticky Chat/Call bar (`bottom-[73px]` on mobile to clear MobileBottomNav). Records views via `recordView()` on mount.
  - **Search** — `src/pages/Search.tsx` (route `/search`): default state (photo search, recent/popular/trending) + live typing overlay (product/category/brand matches).
  - **Trends** — `src/pages/Trends.tsx` (route `/home/trends`): trend insights, category chips, featured image, Top Brands, Hot Trends/Keywords, infinite feed. Uses `BuyerTopBar` + `MobileBottomNav autoHide`.
  - **Following** — `src/pages/Following.tsx` + `FollowingViewAll.tsx` (routes `/home/followings`, `/home/followings/view-all`) backed by `src/lib/followingStore.ts` (follow/unfollow/hide brands).
  - **For You** — `src/pages/ForYou.tsx` (route `/home/for-you`): 3-step onboarding + preference-filtered feed, backed by `src/lib/preferencesStore.ts` (categories/locations/hasCompleted).
  - **My Profile** — `src/pages/Profile.tsx` rewritten + `ProfileNotifications.tsx`, `ProfileSocialLinks.tsx`, `ProfileAccountPrefs.tsx`, `TermsConditions.tsx` (routes `/profile/notifications`, `/profile/social-links`, `/profile/regional-settings`, `/profile/terms`) backed by `src/lib/profileStore.ts`. Regional settings kept INR/IST per constraint (overrode mockup USD/ET). Terms jurisdiction filled "Mumbai, India".
  - **Wishlist system** — `src/lib/savedStore.ts` (folders + products + pending-modal, seeded), `src/components/buyer/SaveToFolderModal.tsx` (global modal mounted once in `App.tsx` inside `<BrowserRouter>`), `src/pages/SavedCollections.tsx` + `SavedCollectionDetail.tsx` (routes `/saved`, `/saved/:collectionId`). Any card bookmark calls `openSaveModal(product)`. Wired across `BuyerProductCard.tsx`, `ListingProductCard.tsx` (+ `src/lib/listingProducts.ts` types/helpers), `NewArrivals.tsx`, `Trends.tsx`, `Search.tsx`, and `EverydayFashionHero.tsx` (the last two previously used dead local state — fixed).
  - **Create New Requirement** — `src/pages/PostRequirement.tsx` rewritten as a hub → circular category select → **schema-driven per-category form** (`SCHEMAS` map: apparel/fabrics/cosmetics/accessories/labels/software/raw/packaging/trims/designer/marketing/freelancer/exporter/photography each render category-relevant fields) → full-coral success screen auto-redirecting to `/requirement/my-quotes`. Field types: text/textarea/description(voice)/select/chips/radio/priceRange/files. Real Web Speech API voice-to-text on Description.
  - **Recently Viewed** — `src/pages/RecentlyViewed.tsx` rewritten (route `/recently-viewed`) backed by new `src/lib/recentlyViewedStore.ts` (`recordView`/`removeRecent`/`clearRecent`/`useRecentlyViewed`/`relativeTime`, seeded, localStorage). Header trash → "Clear viewing history?" confirm dialog; per-card trash deletes instantly; GENDER/SORT/FILTER bar; heart routes through `openSaveModal`.
  - **MobileBottomNav** — `src/components/layout/MobileBottomNav.tsx` gained optional `autoHide?: boolean` (scroll-direction hide/show).
  - Icon note: `lucide-react` is used across buyer pages (project already depends on it; design skill's lucide-discouragement intentionally overridden).

- 2026-07-01: **Documentation automation** — Added a Stop hook enforcement plan (`.claude/hooks/doc-stop-hook.ps1` + `.claude/settings.json` Stop hook) that blocks session end when `src/` is newer than `changelog.md` (honors `stop_hook_active` loop guard, resolves paths from `$PSScriptRoot`). Backfilled this changelog + CLAUDE.md for the buyer-side session (docs were auto-loaded read-only each turn but never auto-written; only the manual-`/compact` PreCompact agent hook writes).

- 2026-06-17: **Second Vercel deploy** — pushed all animation changes live. Preview URL: `https://textile-spark-469oy8ne3-abhishekmitraaas-projects.vercel.app`. Build: 3048 modules, cached deps. Deployed as preview (`target: null`); production alias `textile-spark-net.vercel.app` points to first deploy. Run `vercel deploy --prod` to promote to production.

- 2026-06-17: **Full vendor side animation pass** — Applied consistent Framer Motion animation system across all 26 vendor-side pages. Animation constants used everywhere:
  ```tsx
  const E = [0.23, 1, 0.32, 1];   // strong ease-out cubic-bezier
  const page = { hidden: {}, show: { transition: { staggerChildren: 0.07, delayChildren: 0.04 } } };
  const section = { hidden: { opacity: 0, y: 18 }, show: { opacity: 1, y: 0, transition: { ease: E, duration: 0.38 } } };
  const listContainer = { show: { transition: { staggerChildren: 0.055 } } };
  const listItem = { hidden: { opacity: 0, y: 10 }, show: { opacity: 1, y: 0, transition: { ease: E, duration: 0.26 } } };
  ```
  Rules: `useReducedMotion()` in every page, `whileTap={{ scale: 0.97 }}` on every button, `whileHover={{ x: 3 }}` on ArrowRight icons, no `transition: all`. Pages animated: `Index.tsx`, `Products.tsx`, `Leads.tsx`, `Quotes.tsx`, `Upload.tsx`, `Advertisements.tsx`, `AdvertisementSlideshow.tsx`, `OldAdvertisements.tsx`, `CompetitorAds.tsx`, `BusinessProfile.tsx`, `BusinessProfileEmployees.tsx`, `MyStore.tsx`, `MyBusiness.tsx`, `BusinessTools.tsx`, `BusinessProfileScorePage.tsx`, `Analytics.tsx`, `Reviews.tsx`, `Subscription.tsx`, `UploadCatalogue.tsx`, `UploadVideo.tsx`, `Help.tsx`, `ReportFraud.tsx`, `AppFeedback.tsx`, `VendorBlogs.tsx`, `VendorBlogArticle.tsx`.

- 2026-06-17: **`src/pages/AddSocialLinks.tsx` redesign** — Full redesign using `design-taste-frontend` skill. Was 480 lines of inline `style={}` objects. Changes: added missing `DashboardLayout` wrapper (CLAUDE.md constraint), removed fixed `maxWidth: 390px` → `max-w-xl mx-auto`, removed Google Fonts `@import`, converted all inline styles to Tailwind, each social platform gets its own card (`bg-white rounded-2xl border border-gray-100 shadow-sm`), added `description` field to `SocialPlatform` interface, `PlatformCard` component with `useReducedMotion()` internally, completion `CheckCircle2` springs in when any URL filled, field add/remove animates with height 0→auto + opacity, save button morphs between "Save Social Links" and "Saved ✓" via `AnimatePresence mode="wait"`, header shows live progress (`X of N platforms connected`).

- 2026-06-17: **`src/pages/SellerHome.tsx` animation rewrite** — Applied Emil Kowalski philosophy via `emil-design-eng` skill. Page wrapper uses `variants={page}` stagger, all 11 sections use `variants={section}`, stat cards / lead cards / messages use `listContainer`/`listItem` stagger, carousel `transition-all` replaced with explicit cubic-bezier opacity+transform, `whileTap` on every button, `whileHover={{ x: 3 }}` on ArrowRight, competitor card gets `whileHover={{ scale: 1.012 }}`, `useReducedMotion()` disables all animation for accessibility.

- 2026-06-17: **Vercel deployment** — deployed project live at `https://textile-spark-net.vercel.app` (alias) and `https://textile-spark-fh7ess7qq-abhishekmitraaas-projects.vercel.app`. Build: 3048 modules, 9.76s, ~1.9MB JS bundle. Vercel project `abhishekmitraaas-projects/textile-spark-net`. GitHub auto-deploy not yet connected; manual `vercel --yes` required to redeploy. Installed Vercel CLI globally via `npm install -g vercel`.

- 2026-06-17: **Installed `emil-design-eng` skill** (`emilkowalski/skill`) — encodes Emil Kowalski's philosophy on UI polish, micro-interactions, animation decisions. Invoke with `/emil-design-eng`. Installed to `.agents/skills/emil-design-eng`.

- 2026-06-16: **Created `src/pages/UploadVideo.tsx`** — vendor page at `/upload-video` for uploading product video closeups (shown to buyers in a vertical video feed). Features: drag-and-drop video upload (MP4/MOV/WebM, 200MB max), live phone mockup preview (CSS-only, no library), caption with 150-char counter, product tag picker with AnimatePresence dropdown, custom thumbnail upload, published closeups list with status badges. Key types: `UploadedVideoCloseup`, components: `VideoCloseupPhoneMockup`, `VideoCloseupCard`. Terminology is strictly "Video Closeup" — never "Reel/Reels". Badge color is vendor blue `#256fef`. Wired in `App.tsx` at `/upload-video`. Quick actions grid tile updated to "Video Closeup" (`src/components/dashboard/SellerQuickActionsGrid.tsx`).

- 2026-06-16: **Created `src/pages/UploadCatalogue.tsx`** — vendor page at `/upload-catalogue` for uploading PDF product catalogues. Features: drag-and-drop PDF upload (20MB max), catalogue name/description fields, visibility selector (all buyers / verified only / private), moderation notice (24–48h review), existing catalogues list with status (`live | under_review | rejected`), download/view counters. Key type: `CatalogueFile`. Wired in `App.tsx`. Quick actions grid CTA banner updated to link `/upload-catalogue`.

- 2026-06-16: **Vendor side flow audit & fixes** across multiple files:
  - `src/components/layout/DashboardSidebar.tsx`: Fixed broken `/settings` route — seller "Settings" → `/my-store`, buyer "Settings" → `/profile` (no settings page exists).
  - `src/pages/Quotes.tsx`: Wired all dead action buttons in `SubmittedQuotesView`. Added `onSwitchToRequests` callback prop and `useNavigate`. "Browse RFQs" → switch tab; "Continue Chat" / "View Order Details" → `/chat`; "View RFQ" / "Browse New RFQs" / "Set Alerts" → tab switch or `/leads`.
  - `src/pages/SellerHome.tsx`: Lead card "Call" button → `/leads`; Recent Messages rows made clickable → `/chat` with hover styles.
  - `src/pages/Index.tsx`: Fixed buyer mode "Track Orders" broken route `/orders` → `/requirement/my-quotes`.

- 2026-06-03: Initialized project context notes. Added a standing rule that future sessions should start by reading `memory.md` and `changelog.md` only, instead of scanning full chat history. Captured the current app stack and major routed surfaces in memory for quick recovery.
- 2026-06-09: Created CLAUDE.md with project architecture, development commands, and critical business constraints. Resolved merge conflicts in `src/App.tsx` (lines 51–55, 409–413): kept MyBusiness and BusinessTools imports/routes from HEAD branch. Created memory system: `memory/cosora_platform.md` (business context, three-sided platform), `memory/cosora_constraints.md` (legal/business constraints), and `memory/MEMORY.md` (index). Updated `.github/copilot-instructions.md` with automatic documentation hook: CLAUDE.md, changelog.md, and memory files now auto-update after every chat without prompting, capturing architectural decisions, technical findings, and state changes immediately upon discovery. Enhanced CLAUDE.md with Session Initialization section, Three-Sided Platform Architecture overview, and Architectural Patterns documentation (BuyerRouteShell, UserRoleContext). Updated memory system documentation to reference memory/cosora_platform.md and memory/cosora_constraints.md for deeper context. Created `.instructions.md` files at both cosora lovable root and textile-spark-net directories with agent instructions for Claude Code: Session Initialization (read CLAUDE.md, changelog.md, memory/MEMORY.md), Context Management, mandatory Code Generation Requirements instruction block, Operational Philosophy, and Output Rules. Hook ensures CLAUDE.md, changelog.md, and memory/ files auto-update after each task without prompting.
