/// <reference types="vite/client" />

/** Demo sign-in passwords, defined by vite.config.ts for the dev server only; null in every build. */
declare const __DEMO_PASSWORDS__: { buyer: string; vendor: string; admin: string } | null;
