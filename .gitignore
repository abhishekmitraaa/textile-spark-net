# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — do not commit real keys (.env.example is the tracked template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Playwright artifacts. Wiped at the start of every run, so nothing here is durable
# evidence — screenshots meant for documentation/test.md go in screenshots/ instead.
# video-closeups-bunny.spec.ts also stages a ~4 MB video fixture under here.
test-results/
playwright-report/
